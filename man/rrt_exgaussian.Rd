% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rt_exgaussian.R, R/rt_exgaussian_brms.R
\name{rrt_exgaussian}
\alias{rrt_exgaussian}
\alias{drt_exgaussian}
\alias{rt_exgaussian}
\alias{rt_exgaussian_lpdf_expose}
\alias{rt_exgaussian_stanvars}
\alias{log_lik_rt_exgaussian}
\alias{posterior_predict_rt_exgaussian}
\alias{posterior_epred_rt_exgaussian}
\title{Ex-Gaussian Model (Classical Parameterization)}
\usage{
rrt_exgaussian(n, mu = 0.5, sigma = 0.1, tau = 0.2)

drt_exgaussian(x, mu = 0.5, sigma = 0.1, tau = 0.2, log = FALSE)

rt_exgaussian(
  link_mu = "softplus",
  link_sigma = "softplus",
  link_tau = "softplus"
)

rt_exgaussian_lpdf_expose()

rt_exgaussian_stanvars()

log_lik_rt_exgaussian(i, prep)

posterior_predict_rt_exgaussian(i, prep, ...)

posterior_epred_rt_exgaussian(prep)
}
\arguments{
\item{n}{Number of observations. If \code{length(n) > 1}, the length is taken to be the number required.}

\item{mu}{Mean of the Gaussian component. Must be positive. Range: (0, Inf).}

\item{sigma}{SD of the Gaussian component. Must be positive. Range: (0, Inf).}

\item{tau}{Mean of the exponential component (the tail). Must be positive. Range: (0, Inf).}

\item{x}{Vector of quantiles (observed reaction times).}

\item{log}{Logical; if TRUE, probabilities p are given as log(p).}

\item{link_mu, link_sigma, link_tau}{Character of the type of link used to
model the ex-Gaussian parameters. Defaults to \code{"softplus"} for all three
(see Details).}

\item{i, prep}{For brms' functions to run: index of the observation and a \code{brms} preparation object.}

\item{...}{Additional arguments.}
}
\value{
A \code{brms::custom_family} object.
}
\description{
Density, random generation, and \code{brms} custom family for the Ex-Gaussian
distribution, using the "classical" parameterization familiar to
experimental psychologists, in which \code{mu} and \code{sigma} are the mean and SD
of the Gaussian component alone, and \code{tau} is the mean of the exponential
component (the tail). This is unlike \code{brms}'s built-in \code{exgaussian()}
family, in which \code{mu} indexes the mean of the \emph{entire} distribution
(Gaussian + exponential components combined).

Functions:
\itemize{
\item \code{rrt_exgaussian()}: Simulates random draws from the Ex-Gaussian distribution.
\item \code{drt_exgaussian()}: Computes the density (likelihood) of the Ex-Gaussian distribution.
\item \code{rt_exgaussian()}: Creates a \code{brms::custom_family()} for use in \code{brms} models.
}
}
\details{
The Ex-Gaussian distribution is the sum of an independent Normal (Gaussian)
random variable with mean \code{mu} and SD \code{sigma}, and an Exponential random
variable with mean \code{tau} (rate \code{1 / tau}). Unlike \code{brms}'s built-in
\code{exgaussian()} family - in which \code{mu} indexes the mean of the \emph{entire}
distribution (Gaussian + exponential components combined) - here \code{mu} is
the mean of the Gaussian component alone, so that the mean of the full
distribution is \code{mu + tau}.

This distinction matters because changes in the Gaussian location (\code{mu})
and changes in the exponential tail (\code{tau}) can offset one another at the
level of the overall mean, so effects estimated on \code{brms}'s default \code{mu}
can lead to different (and potentially incorrect) inferences than effects
estimated on this classical \code{mu}.

In the \code{brms} custom family (\code{rt_exgaussian()}), all three parameters use
a \code{"softplus"} link (\code{log(1 + exp(x))}) by default, rather than
\code{"identity"} or \code{"log"}. \code{mu} must remain strictly positive since it
represents the (unobserved) center of the Gaussian component. An
\code{"identity"} link would allow the linear predictor to cross zero or go
negative, which is invalid for all three parameters. A \code{"log"} link would
enforce positivity too, but its curvature explodes as the linear predictor
departs from zero, producing extreme gradients and making priors/sampling
harder to calibrate, especially for \code{mu} and \code{tau}, which are already both
on the RT scale (seconds) and can take comparatively large values.
\code{"softplus"} is positive-constrained like \code{"log"} but behaves almost
linearly (\code{softplus(x) ~ x}) away from zero, making it easier to specify
weakly-informative priors directly on the RT scale while still guaranteeing
valid, strictly positive parameters.
}
\examples{
# Simulate 1000 RTs
rts <- rrt_exgaussian(1000, mu = 0.5, sigma = 0.1, tau = 0.2)
# hist(rts, breaks = 50, main = "Simulated Ex-Gaussian RTs", xlab = "Reaction Time")

}
\references{
\itemize{
\item Matzke, D., & Wagenmakers, E. J. (2009). Psychological interpretation of the ex-Gaussian
and shifted Wald parameters: A diffusion model analysis. \emph{Psychonomic Bulletin & Review},
\emph{16}(5), 798–817. \doi{10.3758/PBR.16.5.798}
}
}
