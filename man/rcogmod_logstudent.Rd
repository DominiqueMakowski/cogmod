% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_logstudent.R
\name{rcogmod_logstudent}
\alias{rcogmod_logstudent}
\alias{dcogmod_logstudent}
\alias{pcogmod_logstudent}
\alias{cogmod_logstudent}
\alias{cogmod_logstudent_lpdf_expose}
\alias{cogmod_logstudent_stanvars}
\alias{log_lik_cogmod_logstudent}
\alias{posterior_predict_cogmod_logstudent}
\alias{posterior_epred_cogmod_logstudent}
\title{Shifted Log-Student-t Model}
\usage{
rcogmod_logstudent(n, mu = -0.7, sigma = 0.4, dof = 5, ndt = 0.2, poutlier = 0)

dcogmod_logstudent(
  x,
  mu = -0.7,
  sigma = 0.4,
  dof = 5,
  ndt = 0.2,
  poutlier = 0,
  log = FALSE
)

pcogmod_logstudent(
  q,
  mu = -0.7,
  sigma = 0.4,
  dof = 5,
  ndt = 0.2,
  poutlier = 0,
  lower.tail = TRUE,
  log.p = FALSE
)

cogmod_logstudent(
  link_mu = "identity",
  link_sigma = "softplus",
  link_dof = "log",
  link_ndt = "log",
  link_poutlier = "logit",
  predict_outliers = FALSE
)

cogmod_logstudent_lpdf_expose()

cogmod_logstudent_stanvars()

log_lik_cogmod_logstudent(i, prep)

posterior_predict_cogmod_logstudent(i, prep, predict_outliers = NULL, ...)

posterior_epred_cogmod_logstudent(prep, predict_outliers = NULL)
}
\arguments{
\item{n}{Number of observations. If \code{length(n) > 1}, the length is taken to be
the number required.}

\item{mu}{Location of the Student-t on the log scale. Any real value.}

\item{sigma}{Scale of the Student-t on the log scale. Must be positive.}

\item{dof}{Degrees of freedom of the Student-t on the log scale. Must be
positive. Smaller is heavier-tailed; \code{dof -> Inf} is \code{\link[=cogmod_lognormal]{cogmod_lognormal()}}.}

\item{ndt}{Non-decision time (shift parameter), in seconds. Must be
non-negative. Represents time for processes such as stimulus encoding and
response execution. Range: [0, Inf).}

\item{poutlier}{Proportion of responses generated by the outlier process
rather than by the decision process. Range: \verb{[0, 1]}. At \code{poutlier = 0} the
distribution reduces to the plain shifted LogNormal.}

\item{x}{Vector of quantiles (observed reaction times).}

\item{log}{Logical; if TRUE, probabilities p are given as log(p).}

\item{q}{Vector of quantiles (reaction times, in seconds).}

\item{lower.tail}{Logical; if TRUE (default), probabilities are \code{P[X <= q]},
otherwise \code{P[X > q]} - the survival, which is what a right-censored
response contributes to the likelihood (see the \emph{Censoring} section).}

\item{log.p}{Logical; if TRUE, probabilities p are given as log(p).}

\item{link_mu, link_sigma, link_dof, link_ndt, link_poutlier}{Link functions for
the parameters.}

\item{predict_outliers}{Logical; whether \code{posterior_predict()} and
\code{posterior_epred()} should include the outlier component. \code{FALSE} (the
default) fixes \code{poutlier} to zero for prediction, so predictions describe
the decision process alone; the likelihood is always the full mixture
either way. See \code{\link[=with_outliers]{with_outliers()}}.}

\item{i, prep}{For brms' functions to run: index of the observation and a \code{brms} preparation object.}

\item{...}{Additional arguments.}
}
\value{
\code{rcogmod_logstudent()} returns a numeric vector of \code{n} simulated
reaction times, in seconds. \code{dcogmod_logstudent()} returns the density at
each element of \code{x} - the log density if \code{log = TRUE} - recycled to the
length of the longest argument. \code{cogmod_logstudent()} returns a
\code{brms::custom_family} object, to put on a \code{brms::bf()} formula.
\code{cogmod_logstudent_stanvars()} returns a \code{brms::stanvars} object holding
the family's Stan \code{functions} block, to pass to \code{brms::brm()}, and
\code{cogmod_logstudent_lpdf_expose()} compiles that Stan code and returns it
as an R function, for checking the density outside of a model. The
remaining functions are \code{brms} post-processing methods, called by \code{brms}
rather than directly: \code{log_lik_cogmod_logstudent()} returns a numeric
vector holding one log-likelihood value per posterior draw for
observation \code{i}, and \code{posterior_predict_cogmod_logstudent()} a draws x 1
matrix of reaction times simulated for observation \code{i}.
\code{posterior_epred_cogmod_logstudent()} returns nothing: the decision time
has no finite mean, so it errors rather than report one - summarise
\code{posterior_predict()} draws instead.
}
\description{
Density, random generation, and \code{brms} custom family for the shifted
Log-Student-t distribution - a \strong{robust LogNormal}. A Log-Student-t
distributed decision time is shifted by a non-decision time \code{ndt}, and a
fixed proportion \code{poutlier} of responses is generated by an outlier process
instead of by the decision process.

Functions:
\itemize{
\item \code{rcogmod_logstudent()}: Simulates random draws.
\item \code{dcogmod_logstudent()}: Computes the density (likelihood).
\item \code{pcogmod_logstudent()}: Computes the cumulative distribution function (CDF) or survival.
\item \code{cogmod_logstudent()}: Creates a \code{brms::custom_family()}.
\item \code{cogmod_logstudent_stanvars()}: Generates the \code{stanvars} to pass to \code{brm()}.
}
}
\details{
\code{log(RT - ndt)} follows a \strong{Student-t} distribution with location \code{mu},
scale \code{sigma} and \code{dof} degrees of freedom. As \code{dof} grows the Student-t
becomes the Normal, so \code{\link[=cogmod_lognormal]{cogmod_lognormal()}} is the \code{dof -> Inf} limit: this
family varies \strong{kurtosis} where \code{\link[=cogmod_loggamma]{cogmod_loggamma()}} varies skew.

\code{dof} is what \code{brms::student()} calls \code{nu}. It is renamed here because
\code{\link[=cogmod_lnr]{cogmod_lnr()}} already spends \code{nuzero} and \code{nuone} on drift rates, and
because \code{brms} recognises the name \code{nu} and supplies opinionated defaults for
it; \code{dof} arrives flat like every other parameter this package defines, so
\code{\link[=cogmod_priors]{cogmod_priors()}} simply fills it.

\code{ndt} and \code{poutlier} mean exactly what they do in \code{\link[=cogmod_lognormal]{cogmod_lognormal()}},
and \code{\link[=with_outliers]{with_outliers()}}, \code{\link[=without_outliers]{without_outliers()}}, \code{\link[=p_outlier]{p_outlier()}} and
\code{\link[=cogmod_priors]{cogmod_priors()}} all work here too. See \code{?rcogmod_lognormal} for why \code{ndt} is
expressed directly in seconds rather than as a fraction of the fastest
observed response, what the outlier component is for, and why its scale is a
constant rather than a \code{dpar}.
}
\section{What the heavy tail is for}{
The outlier component behind \code{poutlier} is a half Normal, which by
construction cannot explain a \emph{slow} response: its density at 5 s is
effectively zero, so a long right tail is the decision family's own business.
\code{\link[=cogmod_loggamma]{cogmod_loggamma()}}'s \code{shape} and \code{\link[=cogmod_invgaussian]{cogmod_invgaussian()}}'s \code{sigmadrift} are
two ways of providing one. \code{dof} is a third, and the most direct: it absorbs
slow contaminants into the likelihood rather than into a mixture component.
At \code{dof = 5} the probability of a decision time beyond 5 s is about five
orders of magnitude larger than the matching LogNormal's.
}

\section{Two things to know before using it}{
\strong{The mean does not exist}, for any finite \code{dof}. \code{E[exp(sigma * T)]} with
\code{T} a Student-t diverges because the t has polynomial tails and \code{exp()}
outruns them - there is no region of the parameter space where this family
has an expectation, unlike \code{\link[=cogmod_logweibull]{cogmod_logweibull()}}, whose mean exists below
\code{sigma = 1}. \code{posterior_epred()} therefore errors rather than returning a
number. The \strong{median is exact}: \code{ndt + exp(mu)}. For anything else,
summarise \code{posterior_predict()} draws.

\strong{The density is unbounded at \code{ndt}.} As \code{RT} approaches \code{ndt} from above
the decision density grows like \verb{1 / (t * |log t|^(dof + 1))}, where a
LogNormal decays to zero. The spike is integrable for every \code{dof > 0}, so
the posterior stays proper, but the likelihood has no maximum and the prior
on \code{ndt} is what keeps the sampler off \code{min(RT)}. This is the same situation
as \code{\link[=cogmod_loggamma]{cogmod_loggamma()}} above \code{sigma * shape = 1}, and the reason
\code{\link[=cogmod_priors]{cogmod_priors()}} is not optional here.

A Student-t is symmetric \strong{on the log scale}, so a small \code{dof} fattens both
tails rather than only the slow one. At \code{dof = 2} some 1.5\% of the decision
distribution falls below 0.05 s, against a LogNormal's 5e-9 - territory
\code{poutlier} also claims, so the two trade off. \code{\link[=cogmod_priors]{cogmod_priors()}} centres \code{dof}
at 6 with 95\% of its mass between 1.5 and 24, which keeps the fast-side spike
under a tenth of a percent while leaving the slow tail worth having.
}

\examples{
rts <- rcogmod_logstudent(1000, mu = -0.7, sigma = 0.4, dof = 5,
                          ndt = 0.2, poutlier = 0.02)
hist(rts, breaks = 100, xlab = "RT (s)")

# A heavier tail than the LogNormal it nests, on the slow side...
dcogmod_logstudent(5, dof = 5, ndt = 0.2)
dcogmod_lognormal(5, ndt = 0.2)

# ...and on the fast side too, which is what `poutlier` also covers.
dcogmod_logstudent(0.21, dof = 5, ndt = 0.2)
dcogmod_lognormal(0.21, ndt = 0.2)

}
\references{
\itemize{
\item Lange, K. L., Little, R. J. A., & Taylor, J. M. G. (1989). Robust
statistical modeling using the t distribution. \emph{Journal of the American
Statistical Association}, \emph{84}(408), 881-896. \doi{10.2307/2290063}
}
}
