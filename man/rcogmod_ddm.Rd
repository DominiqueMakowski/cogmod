% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_ddm.R
\name{rcogmod_ddm}
\alias{rcogmod_ddm}
\alias{dcogmod_ddm}
\alias{cogmod_ddm_lpdf_expose}
\alias{cogmod_ddm_stanvars}
\alias{cogmod_ddm}
\alias{log_lik_cogmod_ddm}
\alias{posterior_epred_cogmod_ddm}
\alias{posterior_predict_cogmod_ddm}
\title{Drift Diffusion Model (DDM)}
\usage{
rcogmod_ddm(
  n,
  drift,
  boundary,
  bias,
  ndt,
  sigmadrift = 0,
  sigmabias = 0,
  sigmatau = 0,
  minrt = NULL,
  ...
)

dcogmod_ddm(
  x,
  drift,
  boundary,
  bias,
  ndt,
  response,
  log = FALSE,
  sigmadrift = 0,
  sigmabias = 0,
  sigmatau = 0,
  minrt = NULL,
  ...
)

cogmod_ddm_lpdf_expose()

cogmod_ddm_stanvars()

cogmod_ddm(
  link_mu = "identity",
  link_boundary = "softplus",
  link_bias = "logit",
  link_tau = "logit",
  link_minrt = "identity",
  link_sigmadrift = "softplus",
  link_sigmabias = "logit",
  link_sigmatau = "logit"
)

log_lik_cogmod_ddm(i, prep, ...)

posterior_epred_cogmod_ddm(prep)

posterior_predict_cogmod_ddm(i, prep, ...)
}
\arguments{
\item{n}{Number of simulated trials. If \code{length(n) > 1}, the length is taken
to be the number required.}

\item{drift}{Drift rate. Can take any real value.}

\item{boundary}{Decision threshold (boundary separation). Must be positive.}

\item{bias}{Starting point bias, as a proportion of the boundary separation
\emph{measured from the boundary coded \code{0}}. Must be in (0, 1). See Details.}

\item{ndt}{Non-decision time. Must be non-negative.}

\item{sigmadrift}{Inter-trial variability in drift rate (\code{sv} in the usual notation). Must be
non-negative. Default \code{0} (no variability, i.e. the classic 4-parameter DDM).}

\item{sigmabias}{Inter-trial variability in the starting point, expressed as a fraction (in
\verb{[0, 1)}) of the maximum allowed range, i.e. \code{sw = sigmabias * min(2*bias, 2*(1-bias))}
(\code{sw}/\code{sz} in the usual notation). Default \code{0}.}

\item{sigmatau}{Inter-trial variability in non-decision time, expressed as a fraction of
\code{minrt}, i.e. \code{st0 = sigmatau * minrt} (\code{st0} in the usual notation), with \code{ndt} the lower
bound of the resulting Uniform distribution. Default \code{0}.}

\item{minrt}{Minimum reaction time. Only required when \code{sigmatau > 0} (used to scale
\code{sigmatau} into \code{st0}).}

\item{...}{Other arguments to be passed to \code{\link[brms:Wiener]{brms::rwiener()}} or \code{\link[brms:Wiener]{brms::dwiener()}},}

\item{x}{The observed reaction time (RT).}

\item{response}{The decision indicator (0 or 1). 0 for choice 0, 1 for
choice 1.}

\item{log}{Logical; if TRUE, returns the log-density. Default: FALSE.}

\item{link_mu, link_boundary, link_bias, link_tau, link_minrt}{Link functions for the parameters.}

\item{link_sigmadrift, link_sigmabias, link_sigmatau}{Link functions for the extra
inter-trial variability parameters (fix them to 0, e.g. \code{sigmadrift = 0}, in the
\code{brms::bf()} formula to recover the classic 4-parameter DDM).}

\item{i, prep}{For brms' functions to run: index of the observation and a \code{brms} preparation object.}
}
\description{
The Drift Diffusion Model (DDM) is a widely used model for decision-making tasks.
It assumes that evidence accumulates over time until it reaches one of two decision boundaries.

The 'cogmod_ddm' family in this package is not a full re-implementation of the DDM, but uses of the
\code{brms} wiener family. It is simply a reparametrized version for consistency with the other
race models in the package.
}
\section{Response coding}{
The response coded \code{1} (\code{response} here, \code{dec()} in a \code{brms} formula) is the
\strong{upper} boundary and the response coded \code{0} is the \strong{lower} one, following
\code{brms}'s own \code{wiener()} family. Two consequences are worth keeping in mind
when reading a fitted model:
\itemize{
\item \code{bias} is measured \emph{from the lower boundary}, so \code{bias > 0.5} places the
starting point closer to the response coded \code{1}, and \code{bias < 0.5} closer to
the response coded \code{0}. Since first-passage times are shorter for the nearer
boundary, \code{bias > 0.5} makes responses coded \code{1} \emph{faster} than responses
coded \code{0}, and \code{bias < 0.5} makes them slower. At exactly \code{bias = 0.5} the
two conditional RT distributions are identical, whatever the drift rate.
\item A positive \code{drift} pushes the accumulator towards the response coded \code{1}.
When \code{0} codes correct responses and \code{1} codes errors (a common choice when
the task has no natural stimulus-to-boundary mapping), good performance
therefore corresponds to a \emph{negative} drift rate.
}
}

\section{Implementation}{
The underlying 4-parameter process is simulated and evaluated with
\code{\link[brms:Wiener]{brms::rwiener()}}/\code{\link[brms:Wiener]{brms::dwiener()}} (which require the \code{RWiener} package).
The full 7-parameter model is built on top of these rather than delegated to
another package: between-trial variability is simulated by drawing the
per-trial parameters, and evaluated by combining a closed-form drift
correction with Gauss-Legendre quadrature over the starting point and
non-decision time.
}

\examples{
# Simulate data
# data <- rcogmod_ddm(1000, drift = 0.2, boundary = 1, bias = 0.5, ndt = 0.3)
# hist(data[data$response == 0, "rt"], breaks = 50, main = "Reaction Times", xlab = "RT")
# hist(data[data$response == 1, "rt"], breaks = 50, add = TRUE, col = rgb(1, 0, 0, 0.5))

# Compute density
# dcogmod_ddm(x = c(0.5, 0.7), drift = 0.2, boundary = 1, bias = 0.5, resp = c(0, 1), ndt = 0.3)

}
