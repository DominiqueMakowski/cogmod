% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_ddm.R
\name{rcogmod_ddm}
\alias{rcogmod_ddm}
\alias{dcogmod_ddm}
\alias{pcogmod_ddm}
\alias{cogmod_ddm}
\alias{cogmod_ddm_lpdf_expose}
\alias{cogmod_ddm_stanvars}
\alias{log_lik_cogmod_ddm}
\alias{posterior_predict_cogmod_ddm}
\alias{posterior_epred_cogmod_ddm}
\title{Drift Diffusion Model (DDM)}
\usage{
rcogmod_ddm(
  n,
  drift = 0,
  boundary = 1,
  bias = 0.5,
  ndt = 0.2,
  sigmadrift = 0,
  sigmabias = 0,
  sigmandt = 0,
  poutlier = 0
)

dcogmod_ddm(
  x,
  drift = 0,
  boundary = 1,
  bias = 0.5,
  ndt = 0.2,
  response,
  sigmadrift = 0,
  sigmabias = 0,
  sigmandt = 0,
  poutlier = 0,
  log = FALSE
)

pcogmod_ddm(
  q,
  drift = 0,
  boundary = 1,
  bias = 0.5,
  ndt = 0.2,
  response = NULL,
  poutlier = 0,
  lower.tail = TRUE,
  log.p = FALSE
)

cogmod_ddm(
  link_mu = "identity",
  link_boundary = "softplus",
  link_bias = "logit",
  link_sigmadrift = "softplus",
  link_sigmabias = "logit",
  link_sigmandt = "log",
  link_ndt = "log",
  link_poutlier = "logit",
  predict_outliers = FALSE
)

cogmod_ddm_lpdf_expose()

cogmod_ddm_stanvars()

log_lik_cogmod_ddm(i, prep)

posterior_predict_cogmod_ddm(i, prep, predict_outliers = NULL, ...)

posterior_epred_cogmod_ddm(prep, predict_outliers = NULL)
}
\arguments{
\item{n}{Number of simulated trials. If \code{length(n) > 1}, the length is taken
to be the number required.}

\item{drift}{Drift rate. Any real value; positive pushes the accumulator
towards the boundary coded \code{1}.}

\item{boundary}{Boundary separation. Must be positive.}

\item{bias}{Starting point, as a proportion of the boundary separation
\emph{measured from the boundary coded \code{0}}. Must be in \verb{(0, 1)}.}

\item{ndt}{Non-decision time (shift parameter), in seconds. Must be
non-negative. With \code{sigmandt > 0} it is the \strong{lower bound} of the
between-trial distribution rather than its midpoint.}

\item{sigmadrift}{Between-trial SD of the drift rate (\code{sv}). Must be
non-negative. Default \code{0}.}

\item{sigmabias}{Between-trial start-point range, as a fraction in \verb{[0, 1)}
of the widest range that keeps the start point inside the boundaries:
\code{sw = sigmabias * min(2 * bias, 2 * (1 - bias))}. Default \code{0}.}

\item{sigmandt}{Between-trial range of the non-decision time (\code{st0}), in the
same unit as the data, with \code{ndt} its lower bound. Default \code{0}.}

\item{poutlier}{Proportion of responses generated by the outlier process
rather than by the diffusion. Range: \verb{[0, 1]}.}

\item{x}{The observed reaction time (RT).}

\item{response}{The boundary reached: \code{1} for the upper boundary, \code{0} for the
lower one. This gives the \emph{defective} density that boundary carries, mixed
with the outlier component.}

\item{log}{Logical; if TRUE, returns the log-density. Default: FALSE.}

\item{q}{Vector of quantiles (reaction times, in seconds).}

\item{lower.tail}{Logical; if TRUE (default) the probability is
\code{P(RT <= q)}, otherwise \code{P(RT > q)}. With a \code{response}, both are defective
\itemize{
\item see Details.
}}

\item{log.p}{Logical; if TRUE, probabilities are returned on the log scale.}

\item{link_mu, link_boundary, link_bias}{Link functions for the drift rate, the
boundary separation and the starting point. \code{mu} is the drift: \code{brms}
requires the first distributional parameter of a custom family to be called
\code{mu}.}

\item{link_sigmadrift, link_sigmabias, link_sigmandt}{Link functions for the
between-trial variability parameters. Fix them in the \code{brms::bf()} formula
(e.g. \code{sigmadrift = 0}) to recover the classic 4-parameter DDM.}

\item{link_ndt, link_poutlier}{Link functions for the non-decision time and
the outlier rate.}

\item{predict_outliers}{Logical; whether \code{posterior_predict()} and
\code{posterior_epred()} should include the outlier component. \code{FALSE} (the
default) fixes \code{poutlier} to zero for prediction, so predictions describe
the diffusion alone; the likelihood is always the full mixture either way.
On the prediction methods themselves the default is \code{NULL}, which defers to
the flag carried on the model - see \code{\link[=with_outliers]{with_outliers()}}. See Details.}

\item{i, prep}{For brms' functions to run: index of the observation and a \code{brms} preparation object.}

\item{...}{Additional arguments.}
}
\value{
\code{rcogmod_ddm()} returns a data frame with \code{n} rows and two columns:
\item{rt}{The simulated reaction time.}
\item{response}{The boundary reached, \code{1} for upper and \code{0} for lower,
matching the \code{dec()} coding used by the \code{brms} families.}

\code{dcogmod_ddm()} returns the defective density at each element of \code{x} - the
log density if \code{log = TRUE} - and \code{pcogmod_ddm()} the defective cumulative
probability at each element of \code{q}, both for the response given in
\code{response} and recycled to the length of the longest argument.
\code{cogmod_ddm()} returns a \code{brms::custom_family} object, to put on a
\code{brms::bf()} formula. \code{cogmod_ddm_stanvars()} returns a \code{brms::stanvars}
object holding the family's Stan \code{functions} block, to pass to
\code{brms::brm()}, and \code{cogmod_ddm_lpdf_expose()} compiles that Stan code and
returns it as an R function, for checking the density outside of a model.
The remaining functions are \code{brms} post-processing methods, called by
\code{brms} rather than directly: \code{log_lik_cogmod_ddm()} returns a numeric
vector holding one log-likelihood value per posterior draw for observation
\code{i}, \code{posterior_predict_cogmod_ddm()} a draws x 2 matrix of reaction times
and choices simulated for observation \code{i}, and
\code{posterior_epred_cogmod_ddm()} a draws x observations matrix of expected
reaction times (marginal over the two responses, and only approximate once
the between-trial variability parameters are non-zero).
}
\description{
The Drift Diffusion Model (DDM) describes a two-choice decision as noisy
evidence accumulating between two boundaries until one of them is reached.
The boundary reached is the choice and the time taken is the decision time.
The observed RT is that decision time shifted by a non-decision time \code{ndt},
and a fixed proportion \code{poutlier} of responses is generated by an outlier
process instead of by the diffusion.

Functions:
\itemize{
\item \code{rcogmod_ddm()}: Simulates random draws from the DDM.
\item \code{dcogmod_ddm()}: Computes the density (likelihood).
\item \code{pcogmod_ddm()}: Computes the cumulative distribution function.
\item \code{cogmod_ddm()}: Creates a \code{brms::custom_family()} for use in \code{brms} models.
\item \code{cogmod_ddm_stanvars()}: Generates the \code{stanvars} to pass to \code{brm()}.
\item \code{p_outlier()}: Per-trial posterior probability of being an outlier.
}

\code{pcogmod_ddm()} is the cumulative distribution function: the probability that
a response has been made by time \code{q}. With \code{response = NULL} it is the RT
distribution marginally over the choice; with \code{response} set it is the
\emph{defective} CDF that boundary carries, rising to the probability of that
boundary rather than to one.
}
\section{Response coding}{
The response coded \code{1} (\code{response} here, \code{dec()} in a \code{brms} formula) is the
\strong{upper} boundary and the response coded \code{0} is the \strong{lower} one, following
\code{brms}'s own \code{wiener()} family. Two consequences are worth keeping in mind
when reading a fitted model:
\itemize{
\item \code{bias} is measured \emph{from the lower boundary}, so \code{bias > 0.5} places the
starting point closer to the response coded \code{1}, and \code{bias < 0.5} closer to
the response coded \code{0}. Since first-passage times are shorter for the nearer
boundary, \code{bias > 0.5} makes responses coded \code{1} \emph{faster} than responses
coded \code{0}, and \code{bias < 0.5} makes them slower. At exactly \code{bias = 0.5} the
two conditional RT distributions are identical, whatever the drift rate.
\item A positive \code{drift} pushes the accumulator towards the response coded \code{1}.
When \code{0} codes correct responses and \code{1} codes errors (a common choice when
the task has no natural stimulus-to-boundary mapping), good performance
therefore corresponds to a \emph{negative} drift rate.
}
}

\section{Parameterization}{
\code{ndt} is expressed \strong{directly, in seconds} (through a log link in the \code{brms}
family). Nothing about it is taken from the data: it is not bounded by the
fastest observed response, so a non-decision time that varies by condition or
by participant can exceed the sample minimum wherever the data support it.

Tying \code{ndt} to the fastest observed response would cap it at an order
statistic of the sample, so any condition or participant whose true \code{ndt}
exceeded that response would be inexpressible, and the misfit would surface
as spurious effects on the other parameters. Expressing it directly is what
avoids that.

\code{sigmandt} is the between-trial \emph{range} of the non-decision time (\code{st0} in
the usual notation), expressed directly in the same unit as the data, with
\code{ndt} the lower bound of the resulting Uniform.
}

\section{Between-trial variability}{
\code{sigmadrift}, \code{sigmabias} and \code{sigmandt} extend the classic 4-parameter
process to the full 7-parameter one. Each is legitimately \strong{zero}, and
setting all three to zero in the formula recovers the classic model:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{brms::bf(RT | dec(Error) ~ Condition, sigmadrift = 0, sigmabias = 0,
         sigmandt = 0, ndt ~ 1, poutlier ~ 1, family = cogmod_ddm())
}\if{html}{\out{</div>}}

Writing \code{sigmadrift = 0} \strong{fixes} the parameter; leaving it out of \code{bf()}
altogether \emph{estimates} it, which is not the same thing. All three are hard to
recover even from a lot of data, and each has a flat direction at its own
floor - the link only reaches zero at minus infinity, and the likelihood stops
changing well before then - so \code{\link[=cogmod_priors]{cogmod_priors()}} gives all three deliberately
tight priors. Fixing the ones a design cannot identify is usually better than
estimating them behind a prior.
}

\section{The outlier component}{
A shifted distribution assigns exactly zero density to any response faster
than \code{ndt}, which puts a hard boundary in the likelihood at the fastest
observed RT. Mixing in a component with support over the whole positive line
removes it: every response keeps positive density whatever \code{ndt} is, so the
boundary becomes a finite cost rather than a wall and the log-density stays
smooth and differentiable.

Because this model produces a \strong{choice as well as a time}, the contaminant
has to produce both. It is a guess: the choice is uniform over the two
options, and the RT is a \strong{half Normal} with scale \code{0.2} seconds.

\deqn{f(t, k) = p \frac{1}{K} g(t) + (1 - p) f_k(t - ndt)}

The \code{1 / K} is what keeps the total summing to one over the response options;
without it it would come to \code{1 + poutlier}.

\code{poutlier} is a \emph{rate}, not a classification: the model never labels
individual trials, it estimates what share of them came from elsewhere. Use
\code{\link[=p_outlier]{p_outlier()}} for per-trial posterior probabilities.
}

\section{Reaction times must be in seconds}{
The outlier component's scale is a constant in seconds, and so are the
priors \code{\link[=cogmod_priors]{cogmod_priors()}} supplies. There is no argument for changing the
unit. Millisecond data fails silently rather than loudly - the outlier
component contributes nothing and the min-RT boundary comes back. See the
corresponding section of \code{\link[=cogmod_lognormal]{cogmod_lognormal()}} for the full account, which
applies unchanged here.
}

\section{Implementation}{
The 4-parameter density is the Navarro and Fuss (2009) series, evaluated in
log space and vectorised over parameter sets, so a response in the far tail
has a finite log-density rather than \code{log(0)}. It agrees with
\code{\link[brms:Wiener]{brms::dwiener()}} and with \code{rtdists} to about \code{1e-10} wherever those return a
number, and is about eight times cheaper per element than the former. Draws are
taken by inverting the first-passage CDF, which - unlike \code{\link[brms:Wiener]{brms::rwiener()}},
and unlike \code{rtdists} - vectorises over parameter sets, so the per-call setup
is paid once rather than once per posterior draw. That matters for
\code{\link[rstantools:posterior_predict]{rstantools::posterior_predict()}}, where every draw carries its own parameters; it is
several times faster there and agrees with both packages' samplers to within
sampling error.

The full 7-parameter model is built on top of these rather than delegated to
another package: between-trial variability is simulated by drawing the
per-trial parameters, and evaluated by combining a closed-form drift
correction with Gauss-Legendre quadrature over the starting point and
non-decision time, also in log space.

In Stan the decision component is \code{wiener_lpdf()}, called with its own
non-decision time set to zero because the shift is applied by the mixture
around it, and short-circuited to the cheaper 4- and 5-parameter forms
whenever the start-point and non-decision-time ranges both vanish. The
cheapest of the three, Stan's classic 4-parameter density, is used only where
it is sound: it returns \code{-inf} with \strong{NaN} derivatives once the rescaled
decision time \code{t / boundary^2} falls below about \code{6.6e-4}, or once the density
underflows, and a NaN derivative on a trial the mixture gives no weight to
still turns the gradient of the whole model to NaN. Those calls go to the
\code{sv}-capable form instead, which the two agree with to \code{1e-13} where they
meet.
}

\section{Fitting}{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{f <- brms::bf(RT | dec(Error) ~ Condition, boundary ~ Condition, bias ~ 1,
              sigmadrift = 0, sigmabias = 0, sigmandt = 0,
              ndt ~ 1, poutlier ~ 1, family = cogmod_ddm())
brms::brm(f, data = df,
          prior    = cogmod_priors(f, df),
          init     = cogmod_inits(f, df),
          stanvars = cogmod_stanvars(f))
}\if{html}{\out{</div>}}

Use \code{\link[=cogmod_inits]{cogmod_inits()}} rather than \code{init = 0}. \code{brms} initialises on the
unconstrained scale, so \code{init = 0} puts \code{ndt} at \code{exp(0) = 1} second - above
nearly every sub-second RT, which leaves every response attributed to the
outlier component and the diffusion parameters with no gradient at all.
}

\section{Predictions exclude the outlier component}{
\code{posterior_predict()} and \code{posterior_epred()} describe the \strong{diffusion
alone} by default, as if \code{poutlier} were zero, because the outlier component
is a fixed regularizer rather than a claim about how guesses are distributed.
Use \code{\link[=with_outliers]{with_outliers()}} for the fitted mixture - chiefly for
\code{brms::pp_check()} - and \code{\link[=without_outliers]{without_outliers()}} to go back. \code{log_lik()} is
always the full mixture.
}

\section{Accuracy of \code{pcogmod_ddm()}}{
This CDF is more accurate than the one in \code{rtdists}, which is the usual
reference implementation. Deviation from numerical integration of the
density, at \code{drift = -4}, \code{boundary = 0.6}, \code{bias = 0.25}, by decision time:\tabular{lll}{
   decision time \tab this function \tab \code{rtdists::pdiffusion()} \cr
   0.02 \tab \code{+2.8e-16} \tab \code{+5.5e-04} \cr
   0.05 \tab \code{-1.1e-16} \tab \code{+4.0e-04} \cr
   0.30 \tab \code{-1.1e-16} \tab \code{+1.5e-04} \cr
}


Over a grid of 360 (drift, boundary, bias, boundary-reached, time) cells the
worst deviation from integrating \code{\link[=dcogmod_ddm]{dcogmod_ddm()}} is 1e-15, i.e. rounding. The
choice probabilities are exact to the same order, where
\code{rtdists::pdiffusion(Inf, ...)} is out by up to 1.4e-04. The difference is
not academic: this function exists because \code{\link[=rcogmod_ddm]{rcogmod_ddm()}} inverts it to draw
from, so any error in it would land directly in the draws.

\code{pcogmod_ddm()} covers the classic \strong{4-parameter} DDM plus the outlier
component. The between-trial variability parameters would each need their own
quadrature layer on top - and \code{sigmadrift}, which the density handles with a
closed-form correction, has no such form here - so they are not arguments at
all: passing one is an error rather than a silently wrong number. Integrate
\code{\link[=dcogmod_ddm]{dcogmod_ddm()}} over \code{q} if you need them.

With a \code{response}, \code{pcogmod_ddm()} returns the \strong{defective} CDF
\code{P(RT <= q, choice = response)}, which does not reach one: its limit is the
probability of that boundary, given by \code{pcogmod_ddm(Inf, response = k)}. The
upper tail is then the matching defective survival
\code{P(RT > q, choice = response)}, so the two add to that response's own
probability rather than to one. Marginally (\code{response = NULL}) they add to
one as usual. \code{\link[=pcogmod_rdm]{pcogmod_rdm()}} follows the same convention.
}

\examples{
# Simulate data, with 2\% of trials from the outlier process
data <- rcogmod_ddm(1000,
  drift = 0.5, boundary = 1, bias = 0.5, ndt = 0.2, poutlier = 0.02
)
head(data)

# Responses faster than ndt keep positive density, unlike the unmixed model
dcogmod_ddm(0.1, ndt = 0.2, response = 1, poutlier = 0.02)
dcogmod_ddm(0.1, ndt = 0.2, response = 1, poutlier = 0)

\dontrun{
# Needs cmdstanr and a CmdStan toolchain, which live outside CRAN - see the
# package website to install them. Not run under R CMD check, which executes
# every example in one R session: once brms has fitted a model there (the
# cogmod_inits() and p_outlier() examples do), rstan is live in the process
# and loading an exposed Stan function next to it segfaults on Linux.
lpdf <- cogmod_ddm_lpdf_expose()
lpdf(
  Y = 0.5, mu = 0.5, boundary = 1, bias = 0.5, sigmadrift = 0,
  sigmabias = 0, sigmandt = 0, ndt = 0.2, poutlier = 0.02, dec = 1
)
}

}
\references{
\itemize{
\item Ratcliff, R., & McKoon, G. (2008). The diffusion decision model: Theory and
data for two-choice decision tasks. \emph{Neural Computation}, \emph{20}(4),
873-922. \doi{10.1162/neco.2008.12-06-420}
\item Navarro, D. J., & Fuss, I. G. (2009). Fast and accurate calculations for
first-passage times in Wiener diffusion models. \emph{Journal of Mathematical
Psychology}, \emph{53}(4), 222-230. \doi{10.1016/j.jmp.2009.02.003}
}
}
\seealso{
\code{\link[=rcogmod_rdm]{rcogmod_rdm()}}, \code{\link[=rcogmod_lba2]{rcogmod_lba2()}}, \code{\link[=rcogmod_lnr]{rcogmod_lnr()}}
}
