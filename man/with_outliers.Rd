% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rt_lognormal_brms.R
\name{with_outliers}
\alias{with_outliers}
\alias{without_outliers}
\title{Include or exclude the outlier component in predictions}
\usage{
with_outliers(object)

without_outliers(object)
}
\arguments{
\item{object}{A \code{brmsfit} fitted with the \code{\link[=rt_lognormal]{rt_lognormal()}} family.}
}
\value{
The model, with the flag set. The fit itself is untouched - only how
predictions are summarised changes.
}
\description{
Switches the \code{predict_outliers} flag on a model fitted with \code{\link[=rt_lognormal]{rt_lognormal()}},
controlling whether \code{posterior_predict()} and \code{posterior_epred()} describe the
fitted mixture or the decision process alone.

Predictions \strong{exclude} the outlier component by default, because for almost
every downstream use it is a nuisance: it pulls expected values toward its own
mean (0.331 s at the default \code{minrt}, and proportional to it) and adds a
spike of implausibly fast draws to posterior predictive samples. It is also a deliberately fixed regularizer rather than a
claim about how guesses are distributed, so simulating from it means
simulating from something the model does not assert.

\code{with_outliers()} restores the mixture. The main reason to want it is
\code{brms::pp_check()}: on untrimmed data the decision-only predictive has no fast
spike to match the one in the data, which reads as misfit. Use
\code{pp_check(with_outliers(m))} for a like-for-like check.

The flag is stored \strong{on the model} rather than passed as an argument, because
\code{brms} and the packages built on it (\code{insight}, \code{modelbased},
\code{marginaleffects}, \code{emmeans}) do not forward extra arguments down to a custom
family's prediction methods - \code{posterior_epred()} reaches the family method
with \code{prep} and nothing else. Carrying it on the object is what makes it work
through all of them. The same flag can be set up front with
\code{rt_lognormal(predict_outliers = TRUE)}.

\code{log_lik()} is unaffected and has no equivalent switch: the likelihood \emph{is}
the mixture, and dropping a component from it would not be a different summary
of the same model but a different model. One consequence worth knowing is that
\code{posterior_predict()} and \code{log_lik()} do not describe the same distribution by
default. This also desyncs \code{loo_pit()}, \code{loo_predict()} and \code{bayes_R2()} from
\code{loo()}, not just hand-rolled checks - anything that compares a simulated
replicate against the likelihood should be run on \code{with_outliers()}.
}
\examples{
# m <- brms::brm(bf(RT ~ Condition, ndt ~ 1, poutlier ~ 1,
#                   family = rt_lognormal()),
#                data = df, stanvars = rt_lognormal_stanvars())
#
# # the decision process alone - the default, everywhere downstream
# brms::posterior_epred(m)
# modelbased::estimate_means(m, by = "Condition")
# marginaleffects::avg_predictions(m, by = "Condition")
#
# # the fitted mixture, e.g. for a like-for-like predictive check
# brms::pp_check(with_outliers(m))
# without_outliers(m)  # back to the default

}
