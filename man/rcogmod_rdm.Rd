% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_rdm.R
\name{rcogmod_rdm}
\alias{rcogmod_rdm}
\alias{dcogmod_rdm}
\alias{pcogmod_rdm}
\alias{qcogmod_rdm}
\alias{cogmod_rdm}
\alias{cogmod_rdm_lpdf_expose}
\alias{cogmod_rdm_stanvars}
\alias{log_lik_cogmod_rdm}
\alias{posterior_predict_cogmod_rdm}
\alias{posterior_epred_cogmod_rdm}
\title{Two-Accumulator Racing Diffusion Model (RDM)}
\usage{
rcogmod_rdm(
  n,
  vzero = 3,
  vone = 2,
  boundary = 0.5,
  bias = 0.2,
  ndt = 0.2,
  poutlier = 0
)

dcogmod_rdm(
  x,
  vzero = 3,
  vone = 2,
  boundary = 0.5,
  bias = 0.2,
  ndt = 0.2,
  response = NULL,
  poutlier = 0,
  log = FALSE
)

pcogmod_rdm(
  q,
  vzero = 3,
  vone = 2,
  boundary = 0.5,
  bias = 0.2,
  ndt = 0.2,
  poutlier = 0,
  response = NULL,
  lower.tail = TRUE,
  log.p = FALSE
)

qcogmod_rdm(
  p,
  vzero = 3,
  vone = 2,
  boundary = 0.5,
  bias = 0.2,
  ndt = 0.2,
  poutlier = 0,
  response = NULL,
  scale_p = FALSE,
  lower.tail = TRUE,
  log.p = FALSE,
  interval = c(0, 10)
)

cogmod_rdm(
  link_mu = "softplus",
  link_driftone = "softplus",
  link_sigmabias = "softplus",
  link_boundary = "softplus",
  link_ndt = "log",
  link_poutlier = "logit",
  predict_outliers = FALSE
)

cogmod_rdm_lpdf_expose()

cogmod_rdm_stanvars()

log_lik_cogmod_rdm(i, prep)

posterior_predict_cogmod_rdm(i, prep, predict_outliers = NULL, ...)

posterior_epred_cogmod_rdm(prep)
}
\arguments{
\item{n}{Number of simulated trials. If \code{length(n) > 1}, the length is taken
to be the number required.}

\item{vzero, vone}{Drift rates of the two accumulators (choice 0 and 1). Must
be non-negative; larger means faster. Zero is allowed - such an accumulator
is slow, but it still finishes, and can still win. Range: \verb{[0, Inf)}.}

\item{boundary}{Threshold offset, \code{boundary = b - bias}, where \code{b} is the
decision threshold and \code{bias} the maximum starting point. Must be positive.}

\item{bias}{Maximum starting point. The starting point of each accumulator on
each trial is drawn from \code{Uniform(0, bias)}. Must be non-negative; zero is
allowed and gives the plain Wald race, in which both accumulators start at
\code{0} on every trial. Range: \verb{[0, Inf)}. Called \code{sigmabias} in the \code{brms}
family, to match \code{\link[=cogmod_lba2]{cogmod_lba2()}}.}

\item{ndt}{Non-decision time (shift parameter), in seconds. Represents the
time taken for processes unrelated to the decision (e.g., encoding, motor
response). Must be non-negative. Range: \verb{[0, Inf)}.}

\item{poutlier}{Proportion of responses generated by the outlier process
rather than by the race. Range: \verb{[0, 1]}. At \code{poutlier = 0} the
distribution reduces to the plain shifted RDM.}

\item{x}{The observed reaction time (RT).}

\item{response}{Accumulator whose finishing time is being scored: \code{0} for the
\code{vzero} accumulator, \code{1} for the \code{vone} accumulator. This gives the
\emph{defective} density \code{f_response(x) * S_other(x)}, mixed with the outlier
component, which is what a race likelihood needs. The default \code{NULL}
instead returns the \emph{marginal} density of the RT, ignoring which
accumulator won - the sum of the two.}

\item{log}{Logical; if TRUE, returns the log-density. Default: FALSE.}

\item{q}{Vector of quantiles (reaction times).}

\item{lower.tail}{If \code{TRUE} (default) return \code{P(RT <= q)}, otherwise the
survival \code{P(RT > q)}. With a \code{response}, both are defective - see Details.}

\item{log.p}{If \code{TRUE}, probabilities are returned on the log scale.}

\item{p}{Vector of probabilities. With \code{response = NULL} these are ordinary
probabilities of the marginal RT distribution. With a \code{response} they are
read off the \emph{defective} CDF unless \code{scale_p = TRUE}, so they must be below
the probability of that response; anything above it has no quantile and
comes back \code{NA} with a warning.}

\item{scale_p}{Logical. If \code{TRUE}, \code{p} is taken as a fraction of the chosen
response's own probability rather than of the whole distribution, so that
\code{p = 0.5} is that response's median. This is what a quantile-probability
plot wants. Ignored when \code{response} is \code{NULL}. Default \code{FALSE}.}

\item{interval}{Length-2 numeric giving the initial bracket, in seconds, for
the root search. The upper end is doubled until it covers the requested
probability, so this only affects speed.}

\item{link_mu, link_driftone}{Link functions for the two drift rates. \code{mu} is
\code{vzero}: \code{brms} requires the first distributional parameter of a custom
family to be called \code{mu}, so that is the name the formula and this argument
use, and the drift of accumulator 0 is what it means.}

\item{link_sigmabias, link_boundary}{Link functions for the start-point range
and the threshold offset.}

\item{link_ndt, link_poutlier}{Link functions for the non-decision time and
the outlier rate.}

\item{predict_outliers}{Logical; whether \code{posterior_predict()} should include
the outlier component. \code{FALSE} (the default) fixes \code{poutlier} to zero for
prediction, so predictions describe the race alone; the likelihood is
always the full mixture either way. On the prediction method itself the
default is \code{NULL}, which defers to the flag carried on the model - see
\code{\link[=with_outliers]{with_outliers()}} to change it after fitting. See Details.}

\item{i, prep}{For brms' functions to run: index of the observation and a \code{brms} preparation object.}

\item{...}{Additional arguments.}
}
\value{
\code{rcogmod_rdm()} returns a data frame with \code{n} rows and two columns:
\item{rt}{The simulated reaction time.}
\item{response}{The winning accumulator, coded \code{0} for \code{vzero} and \code{1} for
\code{vone}, matching the \code{dec()} coding used by the \code{brms} families.}

\code{dcogmod_rdm()} returns the density at each element of \code{x} - the log
density if \code{log = TRUE} - \code{pcogmod_rdm()} the cumulative probability at
each element of \code{q}, and \code{qcogmod_rdm()} the quantile at each element of
\code{p}, in seconds. With a \code{response} the latter two are defective, i.e.
scaled to that response's own probability rather than to one. All are
numeric vectors, recycled to the length of the longest argument.
\code{cogmod_rdm()} returns a \code{brms::custom_family} object, to put on a
\code{brms::bf()} formula. \code{cogmod_rdm_stanvars()} returns a \code{brms::stanvars}
object holding the family's Stan \code{functions} block, to pass to
\code{brms::brm()}, and \code{cogmod_rdm_lpdf_expose()} compiles that Stan code and
returns it as an R function, for checking the density outside of a model.
The remaining functions are \code{brms} post-processing methods, called by
\code{brms} rather than directly: \code{log_lik_cogmod_rdm()} returns a numeric
vector holding one log-likelihood value per posterior draw for observation
\code{i}, and \code{posterior_predict_cogmod_rdm()} a draws x 2 matrix of reaction
times and choices simulated for observation \code{i}.
\code{posterior_epred_cogmod_rdm()} returns nothing: the expected reaction time
of a race has no closed form, so it errors rather than report one -
summarise \code{posterior_predict()} draws instead.
}
\description{
The Racing Diffusion Model (RDM) treats a choice as a race between two
diffusion processes, one per response option, each accumulating evidence at
its own rate until it reaches a common threshold. The winner determines both
the observed reaction time and the choice. The observed RT is that decision
time shifted by a non-decision time \code{ndt}, and a fixed proportion \code{poutlier}
of responses is generated by an outlier process instead of by the race.

Functions:
\itemize{
\item \code{rcogmod_rdm()}: Simulates random draws from the RDM.
\item \code{dcogmod_rdm()}: Computes the density (likelihood).
\item \code{pcogmod_rdm()}: Computes the CDF of the reaction time, marginally over the
choice or defectively for one response.
\item \code{qcogmod_rdm()}: Computes the corresponding quantiles.
\item \code{cogmod_rdm()}: Creates a \code{brms::custom_family()} for use in \code{brms} models.
\item \code{cogmod_rdm_stanvars()}: Generates the \code{stanvars} to pass to \code{brm()}.
\item \code{p_outlier()}: Per-trial posterior probability of being an outlier.
}
}
\details{
\code{pcogmod_rdm()} with \code{response = NULL} (the default) describes the RT of the
trial as a whole - whichever accumulator wins, and whether or not the trial
came from the outlier component - since \code{P(min(T0, T1) > q) = S0(q) * S1(q)}.
That is a closed form and is exact.

With a \code{response}, it returns the \strong{defective} CDF
\code{P(RT <= q, choice = response)}, which is what a defective-CDF or
quantile-probability plot needs. It does not reach one: its limit is the
probability of that response, which \code{pcogmod_rdm(Inf, response = k)} gives.
There is no closed form for it, so it is obtained by quadrature over the
defective density: accurate to about \code{1e-8} rather than to machine precision,
and about ten times slower per element (roughly 3 ms against 0.3 ms), since
the marginal is a vectorised closed form and this is a loop.
\code{lower.tail = FALSE} integrates the upper side directly rather than
subtracting, so the defective survival stays accurate into the tail.

\code{qcogmod_rdm()} inverts \code{pcogmod_rdm()} by root-finding, and so inherits its
quadrature error where a \code{response} is given. It is the natural way to get
the RT quantiles of each response for a quantile-probability plot: ask for
\code{p = c(0.1, 0.3, 0.5, 0.7, 0.9)} with \code{scale_p = TRUE}, once per response.
}
\section{Parameterization}{
Each accumulator is a diffusion with drift rate \code{v} and unit diffusion
coefficient, starting from a point \code{z ~ Uniform(0, bias)} drawn afresh on
every trial and finishing when it reaches the threshold \code{b = boundary + bias}.
The distance it has to cover is therefore \code{b - z = boundary + bias - z},
which is what makes \code{boundary} the threshold \emph{offset}: the distance from the
highest possible starting point to the threshold. Its first passage time is
Wald (inverse Gaussian) with the start point integrated out. The observed
reaction time is \code{ndt + min(T_0, T_1)} and the observed choice is whichever
accumulator got there first.

This is the \code{B} parameterization of \code{DMC} and \code{EMC2}, where \code{b = B + A} with
\code{A} the start-point range (\code{bias} here). It is used for a reason rather than for taste: the
threshold has to sit above the highest possible starting point, and writing
the \emph{offset} makes \code{b > A} hold automatically for any positive value. The
alternative - estimating the absolute threshold, as \code{rtdists} does - needs an
order constraint between two estimated parameters, which has to hold in every
cell of the design once either of them carries a predictor. The cost is that
\code{boundary} alone is not the quantity to read off a fitted model; \code{boundary + bias} is.

A drift rate of \strong{exactly zero is allowed}, and is not the same as an
accumulator that never responds: driftless Brownian motion still reaches any
positive level with probability one, so a zero-drift accumulator is slow but
still finishes, and can still win the race.

A start-point range of \strong{exactly zero is allowed} too, and is a model rather
than a degenerate parameter: both accumulators then start at \code{0} on every
trial and the race is between two plain Walds - equation 2 of Tillman et al.
(2020), which is the limit the density already takes. \code{\link[=cogmod_lba1]{cogmod_lba1()}} and
\code{\link[=cogmod_lba2]{cogmod_lba2()}} have always allowed it. Note that this does not make the
\code{sigmabias} direction any better identified - see Fitting below.

\code{ndt} is expressed \strong{directly, in seconds} (through a log link in the \code{brms}
family). Nothing about it is taken from the data: it is not bounded by the
fastest observed response, so a non-decision time that varies by condition or
by participant can exceed the sample minimum wherever the data support it.

Tying \code{ndt} to the fastest observed response would cap it at an order
statistic of the sample, so any condition or participant whose true \code{ndt}
exceeded that response would be inexpressible, and the misfit would surface
as spurious effects on the race parameters. Expressing it directly is what
avoids that.
}

\section{The outlier component}{
A shifted distribution assigns exactly zero density to any response faster
than \code{ndt}, which puts a hard boundary in the likelihood at the fastest
observed RT. Mixing in a component with support over the whole positive line
removes it: every response keeps positive density whatever \code{ndt} is, so the
boundary becomes a finite cost rather than a wall and the log-density stays
smooth and differentiable. That is what makes the direct parameterization of
\code{ndt} workable without taking a bound from the data.

Because this model produces a \strong{choice as well as a time}, the contaminant
has to produce both. It is a guess: the choice is uniform over the two
options, and the RT is a \strong{half Normal} with scale \code{0.2} seconds.

\deqn{f(t, k) = p \frac{1}{K} g(t) + (1 - p) f_k(t - ndt)}

The \code{1 / K} is what keeps the total summing to one over the response options;
without it it would come to \code{1 + poutlier}. The half Normal is used for the
timing because it is \strong{flat at the origin} (zero derivative), so the very
fastest responses - the ones least plausibly decisions - are not starved of
density, and because it dies fast enough above that range to leave the slow
tail to the race itself rather than claiming it.

\code{poutlier} is a \emph{rate}, not a classification: the model never labels
individual trials, it estimates what share of them came from elsewhere. Use
\code{\link[=p_outlier]{p_outlier()}} for per-trial posterior probabilities.
}

\section{Reaction times must be in seconds}{
The outlier component's scale is a constant in seconds, and so are the
priors \code{\link[=cogmod_priors]{cogmod_priors()}} supplies. There is no argument for changing the
unit. Millisecond data fails silently rather than loudly - the outlier
component contributes nothing and the min-RT boundary comes back. See the
corresponding section of \code{\link[=cogmod_lognormal]{cogmod_lognormal()}} for the full account, which
applies unchanged here.
}

\section{Fitting}{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{f <- brms::bf(RT | dec(Error) ~ Condition, driftone ~ Condition,
              sigmabias ~ 1, boundary ~ 1, ndt ~ 1, poutlier ~ 1,
              family = cogmod_rdm())
brms::brm(f, data = df,
          prior    = cogmod_priors(f, df),
          init     = cogmod_inits(f, df),
          stanvars = cogmod_stanvars(f))
}\if{html}{\out{</div>}}

The \code{brms} family names the drift of the first accumulator \code{mu} (as \code{brms}
requires) and that of the second \code{driftone}, and calls the start-point range
\code{sigmabias} to match \code{\link[=cogmod_lba2]{cogmod_lba2()}}, where it denotes the same quantity. Note
that this is \emph{not} the same thing as \code{bias} in \code{\link[=cogmod_ddm]{cogmod_ddm()}}, which is a
relative starting point in \verb{[0, 1]}. Both drifts use a \code{softplus} link with a
lower bound of zero, following \code{\link[=cogmod_invgaussian]{cogmod_invgaussian()}}: a Wald drift must be
non-negative for the accumulator to be a proper first passage time.

Use \code{\link[=cogmod_inits]{cogmod_inits()}} rather than \code{init = 0}. \code{brms} initialises on the
unconstrained scale, so \code{init = 0} puts \code{ndt} at \code{exp(0) = 1} second - above
nearly every sub-second RT, which leaves every response attributed to the
outlier component and the race parameters with no gradient at all.

\code{\link[=cogmod_priors]{cogmod_priors()}} is not a convenience here either. Beyond \code{ndt} and
\code{poutlier}, \code{sigmabias} and \code{boundary} are only weakly identified from each
other, because they enter the threshold only through the sum
\code{b = boundary + sigmabias} and trade off almost freely: on simulated data with
4000 trials the profile log-likelihood varies by only about 3 units as
\code{sigmabias} ranges from 0 to half the threshold, while \code{boundary} slides to
compensate. With flat priors the sampler tends to wander down the
\code{sigmabias -> 0} ridge (the plain Wald race) and produce divergent
transitions, and a \code{softplus} link reaches zero only at minus infinity - a
flat prior over an unbounded flat region, which is an improper posterior.
That the endpoint is now a legal parameter value does not help: the link
never reaches it, so the flat direction is as long as it ever was.
\code{\link[=cogmod_priors]{cogmod_priors()}} fences both off, exactly as it does for \code{\link[=cogmod_lba1]{cogmod_lba1()}},
which shares this parameterisation.

The sum \code{boundary + sigmabias} is well identified either way, so it is the
more trustworthy quantity to interpret and to compare across conditions. The
same caveat applies to \code{\link[=cogmod_lba2]{cogmod_lba2()}}.
}

\section{Predictions exclude the outlier component}{
\code{posterior_predict()} describes the \strong{race alone} by default, as if
\code{poutlier} were zero, because the outlier component is a fixed regularizer
rather than a claim about how guesses are distributed. Use \code{\link[=with_outliers]{with_outliers()}}
for the fitted mixture - chiefly for \code{brms::pp_check()} - and
\code{\link[=without_outliers]{without_outliers()}} to go back. \code{log_lik()} is always the full mixture.

\code{posterior_epred()} is not provided: for a race model the expectation needs
numerical integration per draw and per observation, and users are better off
summarising \code{posterior_predict()} draws.
}

\examples{
# Simulate data, with 2\% of trials from the outlier process
data <- rcogmod_rdm(1000,
  vzero = 2.5, vone = 1.6, boundary = 0.5, bias = 0.2,
  ndt = 0.2, poutlier = 0.02
)
head(data)

# Responses faster than ndt keep positive density, unlike the unmixed model
dcogmod_rdm(0.1, ndt = 0.2, response = 0, poutlier = 0.02)
dcogmod_rdm(0.1, ndt = 0.2, response = 0, poutlier = 0)

# Defective CDF of one response: at q = Inf it is that response's probability
pcogmod_rdm(c(0.4, 0.6, Inf), vzero = 2.5, vone = 1.6, response = 0)

# The RT quantiles of each response, for a quantile-probability plot
sapply(0:1, function(k) {
  qcogmod_rdm(c(0.1, 0.3, 0.5, 0.7, 0.9),
    vzero = 2.5, vone = 1.6, response = k, scale_p = TRUE
  )
})

\dontrun{
# Needs cmdstanr and a CmdStan toolchain, which live outside CRAN - see the
# package website to install them. Not run under R CMD check, which executes
# every example in one R session: once brms has fitted a model there (the
# cogmod_inits() and p_outlier() examples do), rstan is live in the process
# and loading an exposed Stan function next to it segfaults on Linux.
lpdf <- cogmod_rdm_lpdf_expose()
lpdf(
  Y = 0.5, mu = 2, driftone = 1.5, sigmabias = 0.2, boundary = 0.5,
  ndt = 0.2, poutlier = 0.02, dec = 0
)
}

}
\references{
\itemize{
\item Michael, J. R., Schucany, W. R., & Haas, R. W. (1976). Generating Random Variates Using
Transformations with Multiple Roots. \emph{The American Statistician}, \emph{30}(2), 88-90. \doi{10.2307/2683801}
\item Tillman, G., Van Zandt, T., & Logan, G. D. (2020). Sequential sampling models
without random between-trial variability: The racing diffusion model of
speeded decision making. \emph{Psychonomic Bulletin & Review}, \emph{27}, 911-936.
\doi{10.3758/s13423-020-01719-6}
\item Folks, J. L., & Chhikara, R. S. (1978). The inverse Gaussian distribution and its
statistical application-a review. \emph{Journal of the Royal Statistical Society Series B:
Statistical Methodology}, \emph{40}(3), 263-275.
}
}
\seealso{
\code{\link[=rcogmod_invgaussian]{rcogmod_invgaussian()}}, \code{\link[=rcogmod_lnr]{rcogmod_lnr()}}
}
