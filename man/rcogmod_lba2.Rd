% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_lba2.R
\name{rcogmod_lba2}
\alias{rcogmod_lba2}
\alias{dcogmod_lba2}
\alias{cogmod_lba2}
\alias{cogmod_lba2_lpdf_expose}
\alias{cogmod_lba2_stanvars}
\alias{log_lik_cogmod_lba2}
\alias{posterior_predict_cogmod_lba2}
\alias{posterior_epred_cogmod_lba2}
\title{Two-Accumulator Linear Ballistic Accumulator (LBA) Model}
\usage{
rcogmod_lba2(
  n,
  driftzero = 3,
  driftone = 3,
  sigmazero = 1,
  sigmaone = 1,
  sigmabias = 0.5,
  boundary = 0.5,
  ndt = 0.2,
  poutlier = 0
)

dcogmod_lba2(
  x,
  driftzero = 3,
  driftone = 3,
  sigmazero = 1,
  sigmaone = 1,
  sigmabias = 0.5,
  boundary = 0.5,
  ndt = 0.2,
  response,
  poutlier = 0,
  log = FALSE
)

cogmod_lba2(
  link_mu = "identity",
  link_driftone = "identity",
  link_sigmazero = "softplus",
  link_sigmaone = "softplus",
  link_sigmabias = "softplus",
  link_boundary = "softplus",
  link_ndt = "log",
  link_poutlier = "logit",
  predict_outliers = FALSE
)

cogmod_lba2_lpdf_expose()

cogmod_lba2_stanvars()

log_lik_cogmod_lba2(i, prep)

posterior_predict_cogmod_lba2(i, prep, predict_outliers = NULL, ...)

posterior_epred_cogmod_lba2(prep)
}
\arguments{
\item{n}{Number of simulated trials. If \code{length(n) > 1}, the length is taken
to be the number required.}

\item{driftzero, driftone}{Mean drift rate of each accumulator (choice 0 and
1). Any real value; larger means faster. See Details on negative drifts.}

\item{sigmazero, sigmaone}{Between-trial SD of each accumulator's drift rate.
Must be positive.}

\item{sigmabias}{Maximum starting point. The starting point of each
accumulator on each trial is drawn from \code{Uniform(0, sigmabias)}. Must be
non-negative; \code{0} means both accumulators start at zero on every trial, so
only the drift rates vary - the choice counterpart of the recinormal
special case described in \code{\link[=rcogmod_lba1]{rcogmod_lba1()}}.}

\item{boundary}{Threshold offset, so the threshold is
\code{b = boundary + sigmabias}. Must be positive.}

\item{ndt}{Non-decision time (shift parameter), in seconds. Represents the
time taken for processes unrelated to the decision (e.g., encoding, motor
response). Must be non-negative. Range: \verb{[0, Inf)}.}

\item{poutlier}{Proportion of responses generated by the outlier process
rather than by the race. Range: \verb{[0, 1]}. At \code{poutlier = 0} the
distribution reduces to the plain shifted LBA.}

\item{x}{The observed reaction time (RT).}

\item{response}{The winning accumulator (0 or 1). This gives the \emph{defective}
density \code{f_response(x) * S_other(x)}, mixed with the outlier component,
which is what a race likelihood needs.}

\item{log}{Logical; if TRUE, returns the log-density. Default: FALSE.}

\item{link_mu, link_driftone}{Link functions for the two mean drift rates.
\code{mu} is \code{driftzero}: \code{brms} requires the first distributional parameter of
a custom family to be called \code{mu}.}

\item{link_sigmazero, link_sigmaone}{Link functions for the between-trial
drift SDs.}

\item{link_sigmabias, link_boundary}{Link functions for the start-point range
and the threshold offset.}

\item{link_ndt, link_poutlier}{Link functions for the non-decision time and
the outlier rate.}

\item{predict_outliers}{Logical; whether \code{posterior_predict()} should include
the outlier component. \code{FALSE} (the default) fixes \code{poutlier} to zero for
prediction, so predictions describe the race alone; the likelihood is
always the full mixture either way. On the prediction method itself the
default is \code{NULL}, which defers to the flag carried on the model - see
\code{\link[=with_outliers]{with_outliers()}} to change it after fitting. See Details.}

\item{i, prep}{For brms' functions to run: index of the observation and a \code{brms} preparation object.}

\item{...}{Additional arguments.}
}
\value{
\code{rcogmod_lba2()} returns a data frame with \code{n} rows and two columns:
\item{rt}{The simulated reaction time.}
\item{response}{The winning accumulator, coded \code{0} or \code{1}, matching the
\code{dec()} coding used by the \code{brms} families.}

\code{dcogmod_lba2()} returns the defective density at each element of \code{x} -
the log density if \code{log = TRUE} - for the response given in \code{response},
recycled to the length of the longest argument. \code{cogmod_lba2()} returns a
\code{brms::custom_family} object, to put on a \code{brms::bf()} formula.
\code{cogmod_lba2_stanvars()} returns a \code{brms::stanvars} object holding the
family's Stan \code{functions} block, to pass to \code{brms::brm()}, and
\code{cogmod_lba2_lpdf_expose()} compiles that Stan code and returns it as an R
function, for checking the density outside of a model. The remaining
functions are \code{brms} post-processing methods, called by \code{brms} rather than
directly: \code{log_lik_cogmod_lba2()} returns a numeric vector holding one
log-likelihood value per posterior draw for observation \code{i}, and
\code{posterior_predict_cogmod_lba2()} a draws x 2 matrix of reaction times and
choices simulated for observation \code{i}.
\code{posterior_epred_cogmod_lba2()} returns nothing: the expected reaction
time of a race has no closed form, so it errors rather than report one -
summarise \code{posterior_predict()} draws instead.
}
\description{
The Linear Ballistic Accumulator (LBA) treats a choice as a race between two
accumulators that rise \strong{linearly} - no within-trial noise at all - each at a
rate drawn afresh on every trial. The first to reach the threshold determines
both the reaction time and the choice. The observed RT is that decision time
shifted by a non-decision time \code{ndt}, and a fixed proportion \code{poutlier} of
responses is generated by an outlier process instead of by the race.

Functions:
\itemize{
\item \code{rcogmod_lba2()}: Simulates random draws from the LBA.
\item \code{dcogmod_lba2()}: Computes the density (likelihood).
\item \code{cogmod_lba2()}: Creates a \code{brms::custom_family()} for use in \code{brms} models.
\item \code{cogmod_lba2_stanvars()}: Generates the \code{stanvars} to pass to \code{brm()}.
\item \code{p_outlier()}: Per-trial posterior probability of being an outlier.
}
}
\section{Parameterization}{
Accumulator \code{k} starts at a point \code{z ~ Uniform(0, sigmabias)}, drawn afresh on
every trial, and rises at a constant rate \code{v ~ Normal(drift_k, sigma_k)}, also
drawn afresh on every trial, until it reaches the threshold
\code{b = boundary + sigmabias}. Its finishing time is therefore \code{(b - z) / v}, and
\code{boundary} is the threshold \emph{offset}: the distance from the highest possible
starting point to the threshold. All the randomness is between trials; within
a trial the path is a straight line, which is what makes the density
closed-form.

This is the \code{B} parameterization of \code{DMC} and \code{EMC2}, where \code{b = B + A} with
\code{A} the start-point range. It is used for a reason rather than for taste: the
threshold has to sit above the highest possible starting point, and writing
the \emph{offset} makes \code{b > A} hold automatically for any positive value. The
alternative - estimating the absolute threshold, as \code{rtdists} does - needs an
order constraint between two estimated parameters, which has to hold in every
cell of the design once either of them carries a predictor. The cost is that
\code{boundary} alone is not the quantity to read off a fitted model; \code{boundary + sigmabias} is.

\code{ndt} is expressed \strong{directly, in seconds} (through a log link in the \code{brms}
family). Nothing about it is taken from the data: it is not bounded by the
fastest observed response, so a non-decision time that varies by condition or
by participant can exceed the sample minimum wherever the data support it.

Tying \code{ndt} to the fastest observed response would cap it at an order
statistic of the sample, so any condition or participant whose true \code{ndt}
exceeded that response would be inexpressible, and the misfit would surface
as spurious effects on the race parameters. Expressing it directly is what
avoids that.
}

\section{Negative drift rates}{
A normal drift rate can come out negative, and such an accumulator rises
away from the threshold and never responds. A trial on which \emph{both} drifts
are negative produces no response at all, so it is not a trial: the process
is conditioned on at least one of the two being positive, and
\code{rcogmod_lba2()} draws from exactly that conditional distribution.

The density is conditioned to match, dividing by
\code{1 - pnorm(-driftzero / sigmazero) * pnorm(-driftone / sigmaone)}. Without
that factor the density integrates to the probability of the event rather
than to one, which at low drift rates is a long way short - \code{0.83} at drifts
of \code{0.5} and \code{0.2} with SDs of \code{1.5}, so the likelihood is wrong by 17\% and
wrong by \emph{different} amounts at different parameter values, which is what
makes it bias estimates rather than merely offset them.
}

\section{The evidence scale is arbitrary}{
Multiply \code{driftzero}, \code{driftone}, \code{sigmazero}, \code{sigmaone}, \code{sigmabias} and
\code{boundary} all by any \code{c > 0} and every finishing time \code{(b - z) / v} is
unchanged. The likelihood is therefore \strong{exactly} constant along that ray,
which runs to infinity in both directions: the six parameters are identified
only up to a common scale factor.

\code{\link[=cogmod_priors]{cogmod_priors()}} puts priors on all four positive parameters, which makes
the posterior proper and the sampler well behaved, but a prior does not
\emph{identify} a direction the likelihood cannot see. If the individual
parameters are to be interpreted - rather than the RT distribution they
jointly generate, which is perfectly well identified - fix one SD in the
formula, the usual convention being \code{sigmazero = 1}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{f <- brms::bf(RT | dec(Error) ~ Condition, driftone ~ Condition,
              sigmazero = 1, sigmaone ~ 1, sigmabias ~ 1, boundary ~ 1,
              ndt ~ 1, poutlier ~ 1, family = cogmod_lba2())
}\if{html}{\out{</div>}}

A second, milder flat direction remains either way: \code{sigmabias} and
\code{boundary} enter only through the sum \code{b = boundary + sigmabias}, so they
trade off along a ridge. The sum is the trustworthy quantity to interpret and
to compare across conditions. \code{\link[=cogmod_lba1]{cogmod_lba1()}} and \code{\link[=cogmod_rdm]{cogmod_rdm()}} share it.
}

\section{The outlier component}{
A shifted distribution assigns exactly zero density to any response faster
than \code{ndt}, which puts a hard boundary in the likelihood at the fastest
observed RT. Mixing in a component with support over the whole positive line
removes it: every response keeps positive density whatever \code{ndt} is, so the
boundary becomes a finite cost rather than a wall and the log-density stays
smooth and differentiable.

Because this model produces a \strong{choice as well as a time}, the contaminant
has to produce both. It is a guess: the choice is uniform over the two
options, and the RT is a \strong{half Normal} with scale \code{0.2} seconds.

\deqn{f(t, k) = p \frac{1}{K} g(t) + (1 - p) f_k(t - ndt)}

The \code{1 / K} is what keeps the total summing to one over the response options;
without it it would come to \code{1 + poutlier}.

\code{poutlier} is a \emph{rate}, not a classification: the model never labels
individual trials, it estimates what share of them came from elsewhere. Use
\code{\link[=p_outlier]{p_outlier()}} for per-trial posterior probabilities.
}

\section{Reaction times must be in seconds}{
The outlier component's scale is a constant in seconds, and so are the
priors \code{\link[=cogmod_priors]{cogmod_priors()}} supplies. There is no argument for changing the
unit. Millisecond data fails silently rather than loudly - the outlier
component contributes nothing and the min-RT boundary comes back. See the
corresponding section of \code{\link[=cogmod_lognormal]{cogmod_lognormal()}} for the full account, which
applies unchanged here.
}

\section{Fitting}{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{f <- brms::bf(RT | dec(Error) ~ Condition, driftone ~ Condition,
              sigmazero = 1, sigmaone ~ 1, sigmabias ~ 1, boundary ~ 1,
              ndt ~ 1, poutlier ~ 1, family = cogmod_lba2())
brms::brm(f, data = df,
          prior    = cogmod_priors(f, df),
          init     = cogmod_inits(f, df),
          stanvars = cogmod_stanvars(f))
}\if{html}{\out{</div>}}

The \code{brms} family names the drift of the first accumulator \code{mu} (as \code{brms}
requires) and that of the second \code{driftone}. Use \code{\link[=cogmod_inits]{cogmod_inits()}} rather than
\code{init = 0}: \code{brms} initialises on the unconstrained scale, so \code{init = 0} puts
\code{ndt} at \code{exp(0) = 1} second - above nearly every sub-second RT, which leaves
every response attributed to the outlier component and the race parameters
with no gradient at all.
}

\section{Predictions exclude the outlier component}{
\code{posterior_predict()} describes the \strong{race alone} by default, as if
\code{poutlier} were zero, because the outlier component is a fixed regularizer
rather than a claim about how guesses are distributed. Use \code{\link[=with_outliers]{with_outliers()}}
for the fitted mixture - chiefly for \code{brms::pp_check()} - and
\code{\link[=without_outliers]{without_outliers()}} to go back. \code{log_lik()} is always the full mixture.

\code{posterior_epred()} is not provided: for a race model the expectation needs
numerical integration per draw and per observation, and users are better off
summarising \code{posterior_predict()} draws.
}

\examples{
# Simulate data, with 2\% of trials from the outlier process
data <- rcogmod_lba2(1000,
  driftzero = 3, driftone = 2, sigmazero = 1, sigmaone = 1,
  sigmabias = 0.5, boundary = 0.5, ndt = 0.2, poutlier = 0.02
)
head(data)

# Responses faster than ndt keep positive density, unlike the unmixed model
dcogmod_lba2(0.1, ndt = 0.2, response = 0, poutlier = 0.02)
dcogmod_lba2(0.1, ndt = 0.2, response = 0, poutlier = 0)

\dontrun{
# Needs cmdstanr and a CmdStan toolchain, which live outside CRAN - see the
# package website to install them. Not run under R CMD check, which executes
# every example in one R session: once brms has fitted a model there (the
# cogmod_inits() and p_outlier() examples do), rstan is live in the process
# and loading an exposed Stan function next to it segfaults on Linux.
lpdf <- cogmod_lba2_lpdf_expose()
lpdf(
  Y = 0.5, mu = 3, driftone = 2, sigmazero = 1, sigmaone = 1,
  sigmabias = 0.5, boundary = 0.5, ndt = 0.2, poutlier = 0.02, dec = 0
)
}

}
\references{
\itemize{
\item Brown, S. D., & Heathcote, A. (2008). The simplest complete model of choice response time:
Linear ballistic accumulation. \emph{Cognitive Psychology}, \emph{57}(3), 153-178.
\doi{10.1016/j.cogpsych.2007.12.002}
}
}
\seealso{
\code{\link[=rcogmod_lba1]{rcogmod_lba1()}}, \code{\link[=rcogmod_rdm]{rcogmod_rdm()}}, \code{\link[=rcogmod_lnr]{rcogmod_lnr()}}
}
