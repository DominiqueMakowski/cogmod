% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_exgaussian.R
\name{rcogmod_exgaussian}
\alias{rcogmod_exgaussian}
\alias{dcogmod_exgaussian}
\alias{pcogmod_exgaussian}
\alias{cogmod_exgaussian}
\alias{cogmod_exgaussian_lpdf_expose}
\alias{cogmod_exgaussian_stanvars}
\alias{log_lik_cogmod_exgaussian}
\alias{posterior_predict_cogmod_exgaussian}
\alias{posterior_epred_cogmod_exgaussian}
\title{Ex-Gaussian Model (Classical Parameterization)}
\usage{
rcogmod_exgaussian(n, mu = 0.5, sigma = 0.1, tau = 0.2)

dcogmod_exgaussian(x, mu = 0.5, sigma = 0.1, tau = 0.2, log = FALSE)

pcogmod_exgaussian(
  q,
  mu = 0.5,
  sigma = 0.1,
  tau = 0.2,
  lower.tail = TRUE,
  log.p = FALSE
)

cogmod_exgaussian(
  link_mu = "identity",
  link_sigma = "softplus",
  link_tau = "softplus"
)

cogmod_exgaussian_lpdf_expose()

cogmod_exgaussian_stanvars()

log_lik_cogmod_exgaussian(i, prep)

posterior_predict_cogmod_exgaussian(i, prep, ...)

posterior_epred_cogmod_exgaussian(prep)
}
\arguments{
\item{n}{Number of observations. If \code{length(n) > 1}, the length is taken to be the number required.}

\item{mu}{Mean of the Gaussian component. Unbounded - it is a location, not a
scale - though for RT data it is normally positive. Range: (-Inf, Inf).}

\item{sigma}{SD of the Gaussian component. Must be positive. Range: (0, Inf).}

\item{tau}{Mean of the exponential component (the tail). Must be positive. Range: (0, Inf).}

\item{x}{Vector of quantiles (observed reaction times).}

\item{log}{Logical; if TRUE, probabilities p are given as log(p).}

\item{q}{Vector of quantiles (reaction times, in seconds).}

\item{lower.tail}{Logical; if TRUE (default), probabilities are \code{P[X <= q]},
otherwise \code{P[X > q]} - the survival, which is what a right-censored
response contributes to the likelihood under \code{brms::bf(rt | cens(x) ~ ...)}.
See the \emph{Censoring} section of \code{\link[=rcogmod_invgaussian]{rcogmod_invgaussian()}}.}

\item{log.p}{Logical; if TRUE, probabilities p are given as log(p).}

\item{link_mu, link_sigma, link_tau}{Character of the type of link used to
model the ex-Gaussian parameters. Defaults to \code{"identity"} for \code{mu} and
\code{"softplus"} for \code{sigma} and \code{tau} (see Details).}

\item{i, prep}{For brms' functions to run: index of the observation and a \code{brms} preparation object.}

\item{...}{Additional arguments.}
}
\value{
\code{rcogmod_exgaussian()} returns a numeric vector of \code{n} simulated
reaction times, in seconds. \code{dcogmod_exgaussian()} returns the density at
each element of \code{x} - the log density if \code{log = TRUE} - recycled to the
length of the longest argument. \code{pcogmod_exgaussian()} returns the
cumulative probability at each element of \code{q}, honouring \code{lower.tail} and
\code{log.p}. \code{cogmod_exgaussian()} returns a
\code{brms::custom_family} object, to put on a \code{brms::bf()} formula.
\code{cogmod_exgaussian_stanvars()} returns a \code{brms::stanvars} object holding
the family's Stan \code{functions} block, to pass to \code{brms::brm()}, and
\code{cogmod_exgaussian_lpdf_expose()} compiles that Stan code and returns it
as an R function, for checking the density outside of a model. The
remaining functions are \code{brms} post-processing methods, called by \code{brms}
rather than directly: \code{log_lik_cogmod_exgaussian()} returns a numeric
vector holding one log-likelihood value per posterior draw for
observation \code{i}, \code{posterior_predict_cogmod_exgaussian()} a draws x 1
matrix of reaction times simulated for observation \code{i}, and
\code{posterior_epred_cogmod_exgaussian()} a draws x observations matrix of
expected reaction times.
}
\description{
Density, random generation, and \code{brms} custom family for the Ex-Gaussian
distribution, using the "classical" parameterization familiar to
experimental psychologists, in which \code{mu} and \code{sigma} are the mean and SD
of the Gaussian component alone, and \code{tau} is the mean of the exponential
component (the tail). This is unlike \code{brms}'s built-in \code{exgaussian()}
family, in which \code{mu} indexes the mean of the \emph{entire} distribution
(Gaussian + exponential components combined).

Functions:
\itemize{
\item \code{rcogmod_exgaussian()}: Simulates random draws from the Ex-Gaussian distribution.
\item \code{dcogmod_exgaussian()}: Computes the density (likelihood) of the Ex-Gaussian distribution.
\item \code{pcogmod_exgaussian()}: Computes the cumulative distribution function (CDF) or survival.
\item \code{cogmod_exgaussian()}: Creates a \code{brms::custom_family()} for use in \code{brms} models.
}
}
\details{
The Ex-Gaussian distribution is the sum of an independent Normal (Gaussian)
random variable with mean \code{mu} and SD \code{sigma}, and an Exponential random
variable with mean \code{tau} (rate \code{1 / tau}). Unlike \code{brms}'s built-in
\code{exgaussian()} family - in which \code{mu} indexes the mean of the \emph{entire}
distribution (Gaussian + exponential components combined) - here \code{mu} is
the mean of the Gaussian component alone, so that the mean of the full
distribution is \code{mu + tau}.

This distinction matters because changes in the Gaussian location (\code{mu})
and changes in the exponential tail (\code{tau}) can offset one another at the
level of the overall mean, so effects estimated on \code{brms}'s default \code{mu}
can lead to different (and potentially incorrect) inferences than effects
estimated on this classical \code{mu}.

In the \code{brms} custom family (\code{cogmod_exgaussian()}), \code{sigma} and \code{tau} use a
\code{"softplus"} link (\code{log(1 + exp(x))}) rather than \code{"log"}. Both are scales
and must be strictly positive for the density to exist at all. A \code{"log"} link
would enforce that too, but its curvature explodes as the linear predictor
departs from zero, producing extreme gradients and making priors and sampling
harder to calibrate - and \code{tau} is on the RT scale (seconds), where it can
take comparatively large values. \code{"softplus"} is positive-constrained like
\code{"log"} but behaves almost linearly (\code{softplus(x) ~ x}) away from zero, so
weakly-informative priors can be stated directly on the RT scale.

\code{mu} is different, and uses \code{"identity"}. It is the \strong{location} of the
Gaussian component, not a scale: the convolution is well defined for any real
value, and the density integrates to one at \code{mu = 0} or below just as it does
above (the Stan \code{lpdf} has always accepted a non-positive \code{mu} - it checks
only \code{sigma} and \code{tau}). Nothing is gained by constraining it, and two things
are lost. First, interpretability, which is most of the reason to prefer the
ex-Gaussian in the first place: behind a \code{softplus} link a coefficient is not
in seconds, and the conversion factor moves with the intercept - the local
slope is 0.33 at \code{mu = 0.4} s, 0.39 at 0.5 s and 0.63 at 1 s, so the same
effect reads as a different number depending on where the intercept sits. On
\code{"identity"} a coefficient is seconds, full stop. Second, fidelity: for fast,
heavily-tailed data the Gaussian component genuinely belongs near or below
zero with \code{tau} carrying the mass, and forcing \code{mu > 0} distorts the \code{mu}/\code{tau}
split in exactly the cases where that decomposition is the thing being
estimated. This also matches every other implementation - \code{brms}'s own
\code{exgaussian()}, \code{retimes}, and the estimates reported in the literature - so
fitted values are directly comparable.

The cost is that \code{brms}'s default intercept prior is no longer sensible for
\code{mu} (\code{student_t(3, 0, 2.5)} centred at zero seconds), which is why
\code{\link[=cogmod_priors]{cogmod_priors()}} now supplies one.
}
\examples{
# Simulate 1000 RTs
rts <- rcogmod_exgaussian(1000, mu = 0.5, sigma = 0.1, tau = 0.2)
hist(rts, breaks = 50, main = "Simulated Ex-Gaussian RTs", xlab = "Reaction Time")

}
\references{
\itemize{
\item Matzke, D., & Wagenmakers, E. J. (2009). Psychological interpretation of the ex-Gaussian
and shifted Wald parameters: A diffusion model analysis. \emph{Psychonomic Bulletin & Review},
\emph{16}(5), 798-817. \doi{10.3758/PBR.16.5.798}
}
}
