% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shifted_wald.R, R/shifted_wald_brms.R
\name{rshifted_wald}
\alias{rshifted_wald}
\alias{dshifted_wald}
\alias{pshifted_wald}
\alias{shifted_wald_lpdf_expose}
\alias{shifted_wald_stanvars}
\alias{shifted_wald}
\alias{log_lik_shifted_wald}
\alias{posterior_predict_shifted_wald}
\alias{posterior_epred_shifted_wald}
\title{Shifted Wald Model (Inverse Gaussian)}
\usage{
rshifted_wald(n, drift = 3, bs = 0.5, ndt = 0.2)

dshifted_wald(x, drift = 3, bs = 0.5, ndt = 0.2, log = FALSE)

pshifted_wald(
  q,
  drift = 3,
  bs = 0.5,
  ndt = 0.2,
  lower.tail = TRUE,
  log.p = FALSE
)

shifted_wald_lpdf_expose()

shifted_wald_stanvars()

shifted_wald(
  link_mu = "log",
  link_bs = "log",
  link_tau = "logit",
  link_minrt = "identity"
)

log_lik_shifted_wald(i, prep)

posterior_predict_shifted_wald(i, prep, ...)

posterior_epred_shifted_wald(prep)
}
\arguments{
\item{n}{Number of observations. If \code{length(n) > 1}, the length is taken to be the number required.}

\item{drift}{Drift rate. Must be positive. Represents the average speed of evidence accumulation.
Range: (0, Inf).}

\item{bs}{Decision threshold (boundary separation). Must be positive. Represents the amount of evidence needed
to make a decision. Range: (0, Inf).}

\item{ndt}{Non-decision time (shift parameter). Must be non-negative. Represents time for
processes like stimulus encoding and response execution. Range: [0, Inf).}

\item{x}{Vector of quantiles (observed reaction times).}

\item{log}{Logical; if TRUE, probabilities p are given as log(p).}

\item{q}{Vector of quantiles (observed reaction times).}

\item{lower.tail}{Logical; if TRUE (default), probabilities are \code{P[X <= x]}, otherwise, \code{P[X > x]}.}

\item{log.p}{Logical; if TRUE, probabilities p are given as log(p). Defaults to FALSE.}

\item{link_mu, link_alpha, link_tau, link_minrt}{Link functions.}

\item{i, prep}{For brms' functions to run: index of the observation and a \code{brms} preparation object.}

\item{...}{Additional arguments.}
}
\value{
\code{rshifted_wald} returns a vector of random deviates.
}
\description{
Density, distribution function, and random generation for the Shifted Wald
distribution, also known as the Shifted Inverse Gaussian distribution. This
distribution is commonly used in modeling reaction times in cognitive tasks.
It is characterized by a drift rate (\code{drift}), a decision threshold (\code{bs}),
and a non-decision time (\code{ndt}).

Functions:
\itemize{
\item \code{rshifted_wald()}: Simulates random draws from the Shifted Wald model.
\item \code{dshifted_wald()}: Computes the density (likelihood) of the Shifted Wald distribution.
\item \code{pshifted_wald()}: Computes the cumulative distribution function (CDF).
}
}
\details{
The Shifted Wald distribution describes the time it takes for a Wiener diffusion
process starting at 0 to reach a threshold \code{bs} > 0, given a positive drift
rate \code{drift} > 0. The resulting time is then shifted by a non-decision time \code{ndt} >= 0.

The distribution is mathematically equivalent to shifting an Inverse Gaussian
distribution with mean \code{mu = bs / drift} and shape parameter \code{lambda = bs^2}.
That is, \code{ShiftedWald(drift, bs, ndt) = InverseGaussian(mean = bs/drift, shape = bs^2) + ndt}.

The random generation algorithm implemented here is based on the method described
by Michael, Schucany, and Haas (1976), as used in the \code{statmod} package.
}
\examples{
# Simulate 1000 RTs
rts <- rshifted_wald(1000, drift = 3, bs = 0.5, ndt = 0.2)
hist(rts, breaks = 50, main = "Simulated Shifted Wald RTs", xlab = "Reaction Time")

\dontrun{
# Example brms formula using shifted_wald family
# bf(rt ~ condition + (1|subject),
#    bs ~ 1,
#    tau ~ condition,
#    minrt ~ 1,  # Often fixed or estimated per subject
#    family = shifted_wald())
}
}
\references{
\itemize{
\item Michael, J. R., Schucany, W. R., & Haas, R. W. (1976). Generating Random Variates Using
Transformations with Multiple Roots. \emph{The American Statistician}, \emph{30}(2), 88–90. \doi{10.2307/2683801}
\item Anders, R., Alario, F., & Van Maanen, L. (2016). The shifted Wald distribution for
response time data analysis. \emph{Psychological Methods}, \emph{21}(3), 309–327. \doi{10.1037/met0000063}
\item Matzke, D., & Wagenmakers, E. J. (2009). Psychological interpretation of the ex-Gaussian
and shifted Wald parameters: A diffusion model analysis. \emph{Psychonomic Bulletin & Review},
\emph{16}(5), 798–817. \doi{10.3758/PBR.16.5.798}
\item Folks, J. L., & Chhikara, R. S. (1978). The inverse Gaussian distribution and its
statistical application—a review. \emph{Journal of the Royal Statistical Society Series B:
Statistical Methodology}, \emph{40}(3), 263-275.
}
}
