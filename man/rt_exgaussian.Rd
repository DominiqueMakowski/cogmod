% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rt_exgaussian_brms.R
\name{rt_exgaussian}
\alias{rt_exgaussian}
\alias{rt_exgaussian_lpdf_expose}
\alias{rt_exgaussian_stanvars}
\alias{log_lik_rt_exgaussian}
\alias{posterior_predict_rt_exgaussian}
\alias{posterior_epred_rt_exgaussian}
\title{Ex-Gaussian Model (Classical Parameterization) for brms}
\usage{
rt_exgaussian(
  link_mu = "softplus",
  link_sigma = "softplus",
  link_tau = "softplus"
)

rt_exgaussian_lpdf_expose()

rt_exgaussian_stanvars()

log_lik_rt_exgaussian(i, prep)

posterior_predict_rt_exgaussian(i, prep, ...)

posterior_epred_rt_exgaussian(prep)
}
\arguments{
\item{link_mu, link_sigma, link_tau}{Character of the type of link used to
model the ex-Gaussian parameters. Defaults to \code{"softplus"} for all three
(see Details).}

\item{i, prep}{For brms' functions to run: index of the observation and a \code{brms} preparation object.}

\item{...}{Additional arguments.}
}
\value{
A \code{brms::custom_family} object.
}
\description{
Provides the necessary functions to use the "classical" parameterization
of the Ex-Gaussian distribution as a custom family in \code{brms}. Unlike
\code{brms}'s built-in \code{exgaussian()} family - in which \code{mu} indexes the mean of
the \emph{entire} distribution (Gaussian + exponential components combined) -
this parameterization follows the convention familiar to experimental
psychologists, in which \code{mu} and \code{sigma} are the mean and SD of the
Gaussian component alone, and \code{tau} is the mean of the exponential
component (the tail). The mean of the full distribution is \code{mu + tau}.
}
\details{
This distinction matters because changes in the Gaussian location (\code{mu})
and changes in the exponential tail (\code{tau}) can offset one another at the
level of the overall mean, so effects estimated on \code{brms}'s default \code{mu}
can lead to different (and potentially incorrect) inferences than effects
estimated on this classical \code{mu}.

By default, all three parameters use a \code{"softplus"} link
(\code{log(1 + exp(x))}), rather than \code{"identity"} or \code{"log"}. \code{mu} must remain
strictly positive since it represents the (unobserved) center of the
Gaussian component. An \code{"identity"} link would allow the linear predictor
to cross zero or go negative, which is invalid for all three parameters.
A \code{"log"} link would enforce positivity too, but its curvature explodes
as the linear predictor departs from zero, producing extreme gradients
and making priors/sampling harder to calibrate, especially for \code{mu} and
\code{tau}, which are already both on the RT scale (seconds) and can take
comparatively large values. \code{"softplus"} is positive-constrained like
\code{"log"} but behaves almost linearly (\code{softplus(x) ~ x}) away from zero,
making it easier to specify weakly-informative priors directly on the
RT scale while still guaranteeing valid, strictly positive parameters.
}
