% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rt_weibull_brms.R
\name{rrt_weibull}
\alias{rrt_weibull}
\alias{drt_weibull}
\alias{rt_weibull}
\alias{rt_weibull_lpdf_expose}
\alias{rt_weibull_stanvars}
\alias{log_lik_rt_weibull}
\alias{posterior_predict_rt_weibull}
\alias{posterior_epred_rt_weibull}
\title{Shifted Weibull Model}
\usage{
rrt_weibull(n, mu = 2, sigma = 0.5, ndt = 0.2, poutlier = 0, minrt = 0.3)

drt_weibull(
  x,
  mu = 2,
  sigma = 0.5,
  ndt = 0.2,
  poutlier = 0,
  minrt = 0.3,
  log = FALSE
)

rt_weibull(
  link_mu = "softplus",
  link_sigma = "softplus",
  link_ndt = "log",
  link_poutlier = "logit",
  predict_outliers = FALSE,
  minrt = 0.3
)

rt_weibull_lpdf_expose(minrt = 0.3)

rt_weibull_stanvars(minrt = 0.3)

log_lik_rt_weibull(i, prep)

posterior_predict_rt_weibull(i, prep, predict_outliers = NULL, ...)

posterior_epred_rt_weibull(prep, predict_outliers = NULL)
}
\arguments{
\item{n}{Number of observations. If \code{length(n) > 1}, the length is taken to be
the number required.}

\item{mu}{Shape of the Weibull decision time. Must be positive.}

\item{sigma}{Scale of the Weibull decision time. Must be positive.}

\item{ndt}{Non-decision time (shift parameter), in seconds. Must be
non-negative. Represents time for processes such as stimulus encoding and
response execution. Range: [0, Inf).}

\item{poutlier}{Proportion of responses generated by the outlier process
rather than by the decision process. Range: \verb{[0, 1]}. At \code{poutlier = 0} the
distribution reduces to the plain shifted LogNormal.}

\item{minrt}{Fastest reaction time that could plausibly be a real decision,
in the same unit as the data. Used directly as the scale of the outlier
component. A judgement about the task rather than a statistic: it is a
constant, never estimated, is not read off the sample, and does not bound
\code{ndt}. Defaults to \code{0.3}; change it for data that are not in seconds. See
Details.}

\item{x}{Vector of quantiles (observed reaction times).}

\item{log}{Logical; if TRUE, probabilities p are given as log(p).}

\item{link_mu, link_sigma, link_ndt, link_poutlier}{Link functions for the
parameters.}

\item{predict_outliers}{Logical; whether \code{posterior_predict()} and
\code{posterior_epred()} should include the outlier component. \code{FALSE} (the
default) fixes \code{poutlier} to zero for prediction, so predictions describe
the decision process alone; the likelihood is always the full mixture
either way. See \code{\link[=with_outliers]{with_outliers()}}.}

\item{i, prep}{For brms' functions to run: index of the observation and a \code{brms} preparation object.}

\item{...}{Additional arguments.}
}
\description{
Density, random generation, and \code{brms} custom family for the shifted
Weibull distribution. A Weibull-distributed decision time is shifted by a
non-decision time \code{ndt}, and a fixed proportion \code{poutlier} of responses is
generated by an outlier process instead of by the decision process.

Functions:
\itemize{
\item \code{rrt_weibull()}: Simulates random draws.
\item \code{drt_weibull()}: Computes the density (likelihood).
\item \code{rt_weibull()}: Creates a \code{brms::custom_family()}.
\item \code{rt_weibull_stanvars()}: Generates the \code{stanvars} to pass to \code{brm()}.
}
}
\details{
\code{mu} is the \strong{shape} and \code{sigma} the \strong{scale} of the Weibull decision time,
whose mean is \code{sigma * gamma(1 + 1 / mu)}.

\code{ndt}, \code{poutlier} and \code{minrt} mean exactly what they do in \code{\link[=rt_lognormal]{rt_lognormal()}},
and \code{\link[=with_outliers]{with_outliers()}}, \code{\link[=without_outliers]{without_outliers()}}, \code{\link[=p_outlier]{p_outlier()}} and
\code{\link[=cogmod_priors]{cogmod_priors()}} all work here too. See \code{?rrt_lognormal} for why \code{ndt} is
expressed directly in seconds rather than as a fraction of the fastest
observed response, what the half Student-t outlier component is for, and why
\code{minrt} is a constant on the family rather than a \code{dpar}.

Note that the Weibull density is \strong{unbounded at \code{ndt}} whenever the shape
\code{mu < 1}, which makes the likelihood unbounded as \code{ndt} approaches the fastest
response; the outlier component adds density rather than capping it, so it
cannot repair that. Fit with \code{init = 0}. \code{\link[=rt_loggamma]{rt_loggamma()}} nests this family at
\code{shape = 1} and lets the data choose the shape instead of fixing it.
}
\examples{
rts <- rrt_weibull(1000, mu = 2, sigma = 0.5, ndt = 0.3, poutlier = 0.02)
# hist(rts, breaks = 100, xlab = "RT (s)")

# Responses faster than ndt keep positive density, unlike the unmixed model
drt_weibull(0.1, ndt = 0.3, poutlier = 0.02)
drt_weibull(0.1, ndt = 0.3, poutlier = 0)

}
