% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choco.R
\name{rchoco}
\alias{rchoco}
\alias{choco_stanvars}
\alias{choco7d}
\alias{choco}
\alias{choco7}
\alias{posterior_predict_choco7}
\alias{posterior_predict_choco7d}
\title{Choice-Confidence (CHOCO) Model}
\usage{
rchoco(
  n,
  mu = 0.5,
  muleft = 0.5,
  phileft = 3,
  kleft = 0.95,
  muright = NULL,
  phiright = NULL,
  kright = NULL,
  mud = 0,
  phid = 0,
  kd = 0
)

choco_stanvars(type = "choco7d")

choco7d(
  link_mu = "logit",
  link_muleft = "logit",
  link_mud = "identity",
  link_phileft = "softplus",
  link_phid = "identity",
  link_kleft = "logit",
  link_kd = "identity"
)

choco(
  link_mu = "logit",
  link_muleft = "logit",
  link_mud = "identity",
  link_phileft = "softplus",
  link_phid = "identity",
  link_kleft = "logit",
  link_kd = "identity"
)

choco7(
  link_mu = "logit",
  link_muleft = "logit",
  link_muright = "logit",
  link_phileft = "softplus",
  link_phiright = "softplus",
  link_kleft = "logit",
  link_kright = "logit"
)

posterior_predict_choco7(i, prep, ...)

posterior_predict_choco7d(i, prep, ...)
}
\arguments{
\item{n}{Number of random draws.}

\item{mu}{Probability of choosing the right side (relative to the left side).}

\item{muleft}{The center of the left side beta distribution.}

\item{phileft}{The shape parameter of the left side beta distribution.}

\item{kleft}{The cutoff parameter for the left side beta distribution.}

\item{muright}{The center of the right side beta distribution (overridden by \code{mud} if NULL).}

\item{phiright}{The shape parameter of the right side beta distribution (overridden by \code{phid} if NULL).}

\item{kright}{The cutoff parameter for the right side beta distribution (overridden by \code{kd} if NULL).}

\item{mud}{Deviation for \code{muright} on the logit scale relative to \code{muleft} (used if \code{muright} is NULL).}

\item{phid}{Deviation for \code{phiright} as a log-multiplier relative to \code{phileft} (used if \code{phiright} is NULL).}

\item{kd}{Deviation for \code{kright} on the logit scale relative to \code{kleft} (used if \code{kright} is NULL).}

\item{type}{The type of CHOCO model ("choco7" or "choco7d").}

\item{link_mu, link_muleft, link_mud, link_phileft, link_phid, link_kleft, link_kd, link_muright, link_phiright, link_kright}{Character strings specifying the link functions for the parameters.}

\item{i}{Index of the observation.}

\item{prep}{A \code{brms} preparation object.}

\item{...}{Additional arguments.}
}
\description{
Choice-Confidence (CHOCO) distribution to model bimodal analog/Likert scale data (with zeros and ones).
The idea is to have two separate ordered beta distributions (see Kubinec, 2023) modeling the left and the right hand side of the scale.

Regarding Ordered Beta distributions, see:
\itemize{
\item Stan code: https://github.com/saudiwin/ordbetareg/blob/master/beta_logit.stan
\item Python code: https://github.com/saudiwin/ordbetareg_py/blob/main/ordbetareg/model.py
}

The CHOCO model is available following two different parametrizations:
\itemize{
\item \strong{CHOCO7}: A 7-parameter model where the right-side parameters (\code{muright}, \code{phiright}, \code{kright}) are independent of the left-side parameters.
\item \strong{CHOCO7d}: A deviation-based 7-parameter model where the right-side parameters are derived as deviations from the left-side parameters (\code{mud}, \code{phid}, \code{kd}).
}

Functions:
\itemize{
\item \code{rchoco()}: simulates random draws from the CHOCO distribution, which models bimodal analog/Likert scale data.
\item \code{choco_stanvars()}: Generates a \code{stanvars} object to pass custom functions to \code{brms}.
\item \code{choco()}: Creates a custom family to be used with \code{brms}.
}
}
\examples{
hist(rchoco(3000, mu=0.5, muleft=0.5, phileft=3, kleft=0.95), breaks=100)
hist(rchoco(3000, mu=0.6, muleft=0.5, phileft=5, kleft=0.99), breaks=100)
hist(rchoco(3000, mu=0.4, muleft=0.5, phileft=3, kleft=0.85), breaks=100)

choco_stanvars("choco7")
choco7d()
choco7()
}
